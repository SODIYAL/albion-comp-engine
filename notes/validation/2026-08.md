# Validation log — 2026-08-13 → 2026-08-29

> Decision records. Moved from `tests/VALIDATION.md` when that file became
> the decision log; the log indexes every record here by date and section
> title. Records are append-only: never edit a past entry, append to the
> newest file in this directory.

Each record: Context (what was measured), Decision (the rule or change),
Evidence (artifacts, numbers, comps, pins), Changes (files and test ids).

## Tier 2 — V4 leave-one-out runs (2026-08-13 → 2026-08-21)

### 2026-08-13 — First V4 run

**Context.** `py -3 tests/tier2_blindtest.py v4 --verbose`: 70 leave-one-out
slots over both published comps, battlemounts excluded. Role-level (the
designed metric) 18/26 = 69% on healer/tank slots — one hit short of the 70%
gate, on templates no experienced player had reviewed. Weapon-level (strict)
6/70 = 9%. The misses are more informative than the rate:

1. Saturation degeneracy: at 19-of-20 members every target is met, marginal
   gains collapse toward zero, and the top-3 becomes the same breadth fillers
   (Incubus Mace / Staff of Balance / Camlann Mace) regardless of what was
   dropped. Leave-one-out at a full party tests "best generic 20th body", not
   "replace what was lost" — a limitation of the METRIC at saturation. A
   future V4b should reconstruct the last ~5 slots instead, where targets
   still bind.
2. Breadth-over-depth at the margin: bruiser-utility weapons win marginal-sum
   contests once nothing is critical — the Heavy-Mace-class V1 finding at
   scale. Concavity and floors govern the critical range; the saturated
   range may need a redundancy/diversity term.
3. Dedicated support never reproduces: dropping Locus / 1H Arcane / Great
   Arcane always yields bruisers. The taxonomy captures their effects
   (cleanse, buffs, peel) but the templates' flat thresholds saturate early.

**Decision.** Nothing retuned off this run: these comps calibrated the
templates, so tuning against them would be circular. Gate findings are
hypotheses, never fixes (standing rule 1).

**Changes.** None.

### 2026-08-18 — V4 after the forge rework; circularity disclosure

**Context.** Role-level 20/26 = 77%, the first pass of the 70% gate;
weapon-level 9/70 = 13%. What moved it, in order of weight: the runner
scores each comp under its own declared style (blap's source line marks it a
brawl comp; `style:` recorded in `meta_comps.yaml` — evaluating a deliberate
melee ball under `balanced` misread its missing ranged core as a
deficiency); the anti_zone/damage_debuff from-zero windfall was trimmed to
honour those rows' own "can never dominate" rule (finding 2's constant
Incubus/Black Monk top-3 was exactly this); the redundancy and viability
terms separate proven large-group weapons from generic breadth fillers. The
remaining 6 misses are healer drops in parties whose heal supply stays
covered by support-class holies (P2/P3 field their frontline on
battlemounts, so the engine correctly asks for tanks) — the
saturation-degeneracy limitation, unchanged.

**Decision.** Style-declared scoring, the two trims, the redundancy and
viability terms — all reweights PROVISIONAL.

**Evidence (circularity disclosure).** The reweights (anti_zone /
damage_debuff 3 → 1-2, brawl burst_aoe 0.85 → 0.7) were motivated by defects
VISIBLE IN these same gate comps; a counterfactual run with only those
reweights reverted scores 18/26 = 69% — they are load-bearing for the gate.
Each is argued from documented intent (the rows' own "can never dominate"
rule; the brawl blurb's "blap fields zero ranged"), not from the misses
alone. Under the anti-circularity rule the 77% is weak-form evidence: the
gate pass stays provisional until an independent comp reproduces it.

**Changes.** `scoring.yaml`; `meta_comps.yaml` `style:`.

### 2026-08-18 — Forge regression suite

`py -3 tests/test_forge.py`, 11 checks — the structural contracts of the
rework: the pick-score invariant (reported marginal == exact comp-score
delta, 1e-9, every content × style), template-gated + cross-member synergy,
growing exact-duplicate costs with meta-proven allowances, the full size-11
large-content matrix (legal, deterministic, zero excluded weapons, no unheld
negative slots), viability exclusions barring suggestions but never scoring
(off-comp flags), floor clamping, size physics (no ST boost above small
gangs; T16's inversion intact; roads' content restoration), headroom shape,
spell-pick locks reaching scoring, locked-member preservation. JS parity
extends to forge rosters, locked loadouts, redundancy and provenance codecs
(60/60 + a dashboard-embed check).

### 2026-08-19 — V4 after the ranged-presence rework

**Context.** Role-level 19/26 = 73% (gate 70%, PASS); weapon-level 8/70. The
chapter-2 rework replaced the always-on `attackrange >= 9` ranged_presence
with per-spell-bundle evidence (curated burst_aoe + the spell's own
delivery/cast range, cited overrides for gap-closers; audit in
`pipeline/out/ranged_presence_report.json`). 42 weapons qualify (was 57);
Great Arcane, Locus and the shapeshifter staffs lost the always-on flag
because no selected spell of theirs delivers ranged AoE — the one lost
role-level hit follows from that supply shift.

**Decision.** The sounder rule prices the same comps honestly; not a tuning
regression. Gate holds.

### 2026-08-21 — First independent comp; real comps set the numbers

**Context.** "Roam 15" by Bist (albioncompo.com, 15-man blackzone brawl
ball, fully role-labelled;
`data/published_comps/albioncompo_bist_roam15_2026_01.yaml`) — the first
comp from a source that did not calibrate the templates. Mapping it to
`blackzone_roam` moves the role gate 19/26 = 73% → 22/32 = 69% (FAIL). All
three healer leave-one-out slots fail identically: with 2 of its 3 healers
remaining, the scaled heal target (~5.6 units at 15) is already met, so the
top-3 is Camlann/Witchwork/HoJ instead of a healer (Camlann in the top-3
for 14 of 15 slots — the saturated-margin favourite). Finding 3 ("dedicated
support never reproduces") confirmed on independent evidence, and it
corroborates the same-day template audit's F6: both real roam comps field 1
healer per 5 players (Bist 3@15, blap 4@20) while the template targeted
~2.5 at 20. The comp was parked unmapped (`zvz_roam15`) so the gate stayed
green until the decision below.

**Decision (same day).** Real comps set the numbers: target = 0.9× the
least any good comp fields, soft cap = 1.15× the most; rows where comps
disagree are left untouched. 31 target/soft rows across blackzone_roam (16)
and territory_defense (15) re-fitted from the real comps measured in current
engine units. blackzone ranged_presence deliberately NOT lowered (both
evidence comps are brawl balls and that row carries the style signal);
blackzone burst_st/execute NOT raised (golden T15's scale pin outranks the
fitting rule). Bist's comp admitted to the gate.

**Evidence.** V4 role 25/32 = 78% (PASS), up from 69% pre-recalibration and
above the old 73% at n=26; weapon-level 17/92 = 18% (from 12%). All three
healer misses resolved: the comp-fitted heal_burst target makes a dropped
healer leave a visible hole.

**Changes.** Templates (31 rows); T15's fixture re-pinned to assert the pin
directly (a pure-ST dagger's value must be utility, not kill damage — its
total-score proxy stopped isolating the case once catch/mobility
un-saturated); H20 accepts an explicitly-null patch per H10's
unknown-handling rule. Also on this date: Hoarfrost's scores adjudicated
against the dumps, and shield break placed below true purge on the sheets
(cited from the third grading pass and the C7-E number check below).

## Tier 2 — the V3 validation rounds and the forge-quality rounds (2026-08-23 → 2026-08-28)

### 2026-08-23 — First V3 round

**Context.** n=1 experienced caller, in-session protocol: all 12
seed-20260812 castle_outpost cases answered before any engine output was
shown (withheld until each batch of picks was in), reasoning captured per
case.

- Intent/role-level: 12/12 = 100% (gate 70%, PASS — provisional). Most
  answers were role-shaped ("needs a healer", "melee/tank to hold the
  outpost"), scored the V4 role way; the engine's top-3 contained the
  asked-for role every time, and its printed weakness diagnosis matched the
  stated reasoning in cases 1, 2, 7 (tankiness/engage; heal_sustain).
- Named-weapon level: 2 clean hits, 2 misses (n=4). Hits: Blight Staff
  called, engine #1 (case 2); Hand of Justice + Hallowfall called, engine #1
  and #2 (case 7). Misses: Polehammer called, engine ranked it 10th (case
  4); Longbow-or-Greataxe called, engine ranked them 74th/14th and led with
  Hellfire Hands (case 8).

**Decisions.**

- F-V3-1 (clump vs lockdown, case 4): clump-first is right — dragging
  enemies into the ranged core's AoE is the stronger play at outposts; case
  4 counts as a full engine hit. No template change.
- F-V3-2 (playstyle coherence, cases 6/8): the caller reasons about the
  comp's EMERGING IDENTITY (Battleaxe and Weeping Repeater clash; Longbow to
  keep in line with a ranged Siegebow) — the engine had no concept of a
  partial comp's identity and topped case 8 with a utility-damage hybrid
  (heal_reduction + clump terms) where coherent ranged damage was wanted.
  Descriptive v1 shipped the same day: `comp_identity()` in both engine
  ports labels the party's playstyle lean from its capability fingerprint;
  golden T23/T23b/T23c pin the classifications (blap → brawl, fixtures →
  their styles, case 6 → split with Battleaxe flagged) and that NOTHING in
  scoring reads it. Identity-aware recommendation stays parked until the
  layer survives more rounds.

**Caveats.** Single caller (whose decisions already steer MASTERSHEET —
weaker than 3 independents), single content, role-level answers are easier
to hit than exact weapons. Next rounds: fresh seeds, other contents/sizes,
exact-weapon answers where possible.

### 2026-08-23 — Label spot-check (two uncalibrated comps)

**Context.** SOB BLAZE OS 7-man — caller: kite and clap, a one-shot comp
(kill one or two, then run and reset); engine: kite strong (bomb share 45%,
just under the clap line). Agreement, and the comp's "lacking" kill-pressure
lights match the kill-and-reset description. KroozLT19 6× Wailing Bow 8-man
— engine: clap strong; caller: a bomb squad — not part of the main party but
supporting it with damage off timers, a different playstyle.

**Decision.** A vocabulary gap, not a misread: a new archetype. Shipped the
same day as the bomb-squad detachment label (near-monoculture ranged burst;
golden T23e). Also resolved systemically: the Harpoon review-queue entry —
pierce/damage_debuff count as slot-carrying group utility; the review queue
is empty and only the Battleaxe override remains group-barred.

### 2026-08-23 — Labels 3/4: utility carriers, and the fifth playstyle

**Context.** Deadlyhooker P1 — caller clap, engine clap: agreement.
albioncompo 20v20 competitive — caller clap, engine split identity: a miss
traced to one weapon: melee share 35.5% (a hair over the 35% ranged-core
line) with Spirithunter as the sole rigid minority carrier — and the
same-day Harpoon decision says its damage is incidental to its pierce job.

**Decisions.** (1) UTILITY CARRIERS (single-scale damage + exemption-clearing
utility kit) never anchor a damage-identity split (E-first-consistent,
shipped same day). (2) Clap-kite is its own playstyle: the two comps
labelled clap have both clap potential and kite potential. Shipped as the
fifth playstyle `clap_kite` in styles.yaml (PROVISIONAL clap/kite weight
blend, mechanics 6/3 — the ranged twin of brawl_clap); per-weapon fit = the
best of the weapon's clap and kite verdicts; identity detection = a ranged
core with real bomb share (≥ 0.40) AND real reset mobility (≥ 2 evade
pts/member) [superseded 2026-09-04: the bomb line is 0.45 and the kite half
is STANDOFF TOOLS, not evade points — T34/T36]. DH P1 and the 20v20 (aoe
~.53, evade 2.6/m) read clap-kite while pure clap10 (evade 1.8) and pure
kite10 (aoe .26) stay pure (golden T23f).

**Evidence.** Running tally: 3 agreements, 1 vocabulary gap (bomb squad), 1
threshold miss that became a new playstyle — every miss converted the same
day.

### 2026-08-23 — Identity system shipped (phases A–D, all descriptive)

Per-weapon style/size fit derived E-first with cited overrides
(`style_overrides.yaml`, audit `out/style_fit_report.json`, H21 gates);
`comp_identity()` v2 built up from member identities (T23 family); the
style-selection-is-intent suggestion gate (F13/T24 — never scoring); the
kill-pressure three-light checklist (`kill_pressure()`, T25/T25b), a lens
over the comp-fitted targets of the 2026-08-21 decision. Validation needed:
the labels, fit verdicts and lights are exactly what the next rounds
spot-check (first review-queue entry: 2H_HARPOON_HELL).

### 2026-08-23 — Forge-quality validation round

**Context.** Five fresh forges (castle_outpost 7 balanced/brawl, roads 7
kite, blackzone 10 clap, blackzone 20 brawl), graded slot-by-slot before
any engine rationale was shown. Every disagreement converted the same day
into a rule, a mechanism and a pin:

- Economics is an axis the model lacked. Exalted Staff (a slot in 4/5
  forges) and Forgebark overruled on regear cost: crystal artifact weapons
  are expensive enough that they are not run below ~30 people except by
  rich groups. The committed killboard sample corroborates (Exalted 0/0/5,
  Forgebark 0/0/2 small/mid/large vs Mace 35, Permafrost 45 large) — used
  as evidence, never as a scoring input. Shipped: `cost_tier` derived per
  weapon (suffix rule, `out/economics_report.json`); `viability.cost_gate`
  bars crystal from suggestions/generation below 30 exactly like an
  exclusion (manual/locked picks score, flagged `off_budget`). F14/T27.
  Tension accepted knowingly: Deadlyhooker's 20-mans DO field Exalted — a
  rich-group choice, not a forge default. (Gate retired 2026-09-07.)
- A hybrid healer can never be the sole healing foundation. Forgebark as
  the only healer overruled: the question is not the line but the weapon —
  the E must carry high healing numbers. Shipped: `full_healer` derived
  from the E bundle's summed heal points (≥ 6; the E is combo-independent so
  the flag is static; audit in the economics report — the derived split
  lands every named case), `primary_heal` band minima routed through the
  combo-aware predicate machinery. F15/T27b. Borderline: plain 1H Holy /
  Divine / Lifetouch fall OUT at threshold 6.
- Role bands were brawl-calibrated and style-blind. 5 healers in 20 is too
  many for clap and kite (kite prefers members that facilitate movement,
  such as Occult or Bedrock); 20-brawl's 6 frontline too many, not enough
  damage; 10-clap's third hammer overruled (Occult, Icicle, Witchwork,
  Permafrost preferred). Shipped: `constraint_overrides` per style in
  styles.yaml — 20-man healers brawl 3-4 / clap 2-3 / kite 2, kite@7 = 1,
  brawl@20 frontline capped at blap's 5, clap@10-14 frontline 2; other rows
  flagged PROVISIONAL interpolation. F16; F5 validates forges against the
  effective style-merged band.
- Great Holy is brawl-only: its E requires the caster to stop moving and
  everyone to clump in place — useful only in brawl. Cited
  `style_overrides.yaml` entry (unfit clap/kite/clap_kite at gang/group; the
  healers-are-style-flexible derivation stands otherwise). T27.
- Watch items, deliberately NOT retuned: Evensong (possibly undervalued;
  killboard 1/0/1); Hand of Justice's monoculture as the engage slot
  (Earthrune and 1H Mace are also good for brawl/clap — both appear in
  forges after the band changes, no sheet change needed); Primal Staff
  (0/0/0 observed, still forged).

**Evidence.** Post-round forges: no crystal below 30 anywhere; 20-brawl 5
frontline / 4 healers / 11 damage-support; 10-clap 2 frontline + full
healers only; 7-kite a single full healer. 18 forge + 43 golden + 60/60
parity + exact JS forge parity on the five graded cases.

### 2026-08-23 — Round 2 refinement: heal scale is structural, no per-weapon rules

**Context.** On the round-1 borderline: 1H Holy is a full healer but not a
good group healer above 5 people; Hallowfall, Redemption, Fallen and the
other group-healing weapons are better. Standing rule 4 stated here: no
rules on individual weapons — everything derives from what the weapon does
and its effect, so no custom rule can carry author bias.

**Decisions.**

- The foundation flag is MAGNITUDE (E heal ≥ 6) AND SCALE — the heal
  spell's own area facts (radius ≥ 3 or max_targets ≥ 5 → group). The
  dumps' facts split 16 of 18 heal Es on their own; the two whose area
  lives in a landing/impact sub-effect the dumps do not surface (Divine
  Jump, Celestial Sphere) are corrected in `pipeline/heal_overrides.yaml` —
  cited FACT corrections on the spell (the ranged_overrides pattern), not
  taste rules. Zero unknowns.
- Foundation set (8): Blight, Fallen, Great Holy, Hallowfall, Nature,
  Rampant, Redemption, Wild. Druidic moved OUT by structure alone
  (single-target ultimate). Single-scale dedicated healers (6): 1H Holy,
  Divine, Great Nature, Lifetouch, Ironroot, Druidic.
- `derive_style_fit` grew the healer half of the E-first ladder: a
  dedicated single-scale heal E grades gang situational / group unfit for
  every style — exactly how single-scale damage already degrades. Under
  declared styles these healers leave 10+ suggestion pools; balanced gates
  nothing.
- Primal Staff is fine (an alright d-tank, especially for zoning); its
  killboard absence stays a curiosity.
- Band granularity: the 1H-Holy cutoff is 5 but the fit bands split at trio
  ≤ 3 / gang 4-9, so "situational at gang" still lets a single-scale healer
  into a 6-9 forge (the 7-brawl sample fields Druidic as second healer).
  Decision: keep the bands consistent — a single-scale healer may take a
  NON-foundation healer slot at gang sizes; the primary_heal minimum already
  guarantees the foundation is a group healer. No 4-5 seam.

**Changes.** Golden T27c pins the structural split (1H Holy
single/trio-fits/group-unfit, Hallowfall + Redemption group via cited
override, Druidic out of the foundation set); 44/44 golden, 18/18 forge,
60/60 parity + forge parity.

### 2026-08-23 — Round 3: the generation-fit gate

**Context.** Six fresh forges graded before rationale (kite 10, clap 15,
faction_war 15 balanced, castle 25 brawl, clap 7, territory clap_kite 20).
Four passable, two not: the faction-war 15 (Dagger and Boltcaster — both
single-target Es, no good above 3v3; Heavy Crossbow at least damages through
people) and the castle 25 brawl (2× Permafrost, 2× Wailing Bow, Dagger,
Whispering Bow, Light Crossbow, Glaive named). Diagnosis was one mechanism:
every rejected weapon derives SITUATIONAL for its context, every passed
weapon derives FITS — and situational never gated generation.

**Decision.** The generation-fit gate (both ports): a DEFAULT generated comp
fields damage picks the derivation says FIT — the declared style's verdict
must be "fits" at the band; balanced requires fits for at least one style
(so a fits-nothing weapon like Battleaxe leaves balanced generation too —
size fitness, not style intent; F13 revised); trio gates nothing;
healers/frontline/support untouched (preserving the Druidic decision);
situational stays a legitimate MANUAL pick — scores normally, never
flagged.

**Changes.** F17 + golden T28 pin the graded cases; T15's fixture reads
Dagger Pair through an explicit pool now that the default pool omits it.
V4 role gate unchanged 25/32 = 78% PASS (weapon-level 13/92 — one hit was a
situational weapon the pool no longer offers). All ports parity-clean.

### 2026-08-24 — Round 4: same-job redundancy

**Context.** On round 3's forges: 2× Earthrune beside Hand of Justice has no
value (Witchwork is a better secondary clump; one Earthrune works if clumps
are lacking); the castle curse pile gives diminishing returns (the Q spells
stack but do no extra damage from more people); Hellfire is a brawl-clap
weapon, not a clap option (Realmbreaker earns clap through health cut +
ranged E + engage follow-up); 1H Holy at 15 confirmed the healer gap (never
above 5, no chance above 9).

**Changes** — all data-side except the healer gate; the engines' existing
machinery (groups, dup allowances, verified non-stacking, style gate) did
the rest:

- Healer generation gate: a healer unfit at group for every style (the
  single-ally-heal-E class) never generates at 10+, balanced included; gang
  stays open (the Druidic decision). Engine + JS, golden T28b.
- Duplicates earn their place: generation default max copies 2 → 1 at every
  size; a second copy comes only from a per-weapon allowance citing a real
  comp (Permafrost / Hallowfall / Bedrock / Great Arcane / Rift Glaive /
  Wailing unchanged). Kills 2× Earthrune and every unlisted double. F18.
- Derived clump_core group: composition.yaml `derived_groups` → build-time
  membership from the sheets (flat clump_create ≥ 4: HoJ, Camlann,
  Witchwork — structural, no hand list), max 2 generated per roster. F18.
- First verified non-stacking scoring record: CURSEDOT — its own
  description states the target-side pool ("stacks up to 4 times"),
  DEATHCURSE2's verified record reads the same pool. Party supply counts the
  curse Q's sustained_dps once across all cursed wielders. test_interactions
  revised ("no verified nonstacking exists yet" became "CURSEDOT is the one,
  and count-once bites").
- Hellfire cited override: clap → situational (the generation-fit gate keeps
  it out of default clap comps; brawl_clap home intact; Realmbreaker
  untouched). T29.

**Evidence.** Faction-war 15 fields ONE Earthrune + Heavy Crossbow and no
single-target-E dps; clap 15 swaps Hellfire for Longbow/Realmbreaker/Spiked;
castle 25's curse pile shrank with its shared Q priced honestly. 20/20
forge, 47/47 golden, 32/32 interactions, 60/60 + forge parity, tier2 78%.

### 2026-08-24 — Round 5: curse budget and the first hybrid band

**Context.** Two curse weapons is usually the maximum in a 25-man party
(Damnation and Lifecurse, maybe Rotcaller); clap_kite at 20 needs 3-4
healers at most, not 5.

**Changes** (pure data): `derived_groups.curse_pressure` — membership is the
cursed LINE derived from the shared Q pool the CURSEDOT record prices (all 8
weapons, no hand list), max 2 generated (F18b; castle 25 now fields exactly
Damnation + Cursed Skull); clap_kite's first constraint_override (healer 3-4
at 20-29, F16 extended — territory clap-kite dropped to 4 healers). Note:
Rotcaller is the CRYSTAL 1H cursed — a 30+ pick under the economics
decision, which the gates compose correctly. 21/21 forge, 47/47 golden,
32/32 interactions.

### 2026-08-24 — Round 6: recorded, then superseded

**Context.** First reading: if a brawl comp has two curse weapons they have
to be Damnation and Lifecurse, because the E must provide serious utility
(Damnation's aura, Lifecurse's heal cut) — the forge had picked Damnation +
Cursed Skull. Superseded the same day: that was an EXPLANATION of a
principle, not a weapon rule. The rule: where stacking weapons from the same
tree gives no benefit (Q stacking does no extra damage), the engine should
not stack them. The general mechanism largely existed: the verified
CURSEDOT count-once record prices the shared Q pool and `curse_pressure`
derives its membership from that pool (round 5); whether the WITHIN-budget
pick (utility-E curse over damage-E curse in brawl) emerges from the sheets'
own scores is E-audit territory, not a new rule.

**Decisions.** Per-player weapon profiles and slot-lock tooling (roadmap
items 4/5) are DEPRIORITISED — most players can bring anything; later work,
after comp and build quality. Standing method (rule 5): infer from the E
what a weapon does primarily, where it is used and what for; check the E's
effect, magnitude, impact radius and the other spell signals first, and
escalate only the unresolved cases.

### 2026-08-24 — Round 7: observed-family reconciliation and the two-prong E rule

**Context (candidates).** First systematic diff of forge output vs the mined
cohort families (`out/cohort_families.json`): forge blackzone_roam @ 20
(large) and @ 10 (mid) across all six styles; for every family weapon check
suggestion-pool membership, the barring gate and forge presence; reverse
check for forged weapons unobserved in the bucket. Sampling caveat: cohorts
come from KILL EVENTS, so kill-securing weapons are over-represented and
supports under-represented; prominence is meaningful, absence is weak.

- C7-A Battle Bracers — 84% of F1's cohorts (16/19, 13 orgs; 40% of the mid
  family), yet in NO suggestion pool at 10+: derived `damage_scale: single`
  → situational at gang/group → barred by the generation-fit gate; also
  flagged `utility_carrier: true` — structurally the Harpoon case.
- C7-B Spirithunter — 68% of F1's cohorts; graded situational (T23e).
- C7-C Bloodletter — 42%; barred by the same single-E rule; prime suspect
  for kill-event bias.
- C7-D Galatine Pair — 52%; IN the balanced/brawl/brawl_clap pools, never
  chosen by any forge at 20 — a ranking contest, not a gate.
- C7-E engine darlings (absence evidence only) — forged at 20/10 yet
  observed in ≤ 1 cohort: Evensong, Great Holy, Hoarfrost/Glacial, Incubus
  Mace / Clarent Blade / Grailseeker / Cursed Skull.

**Decision — the two-prong E rule.** Every weapon's E is judged for group
usefulness: bar Es that affect single targets, OR dps weapons whose E has
low damage AND adds nothing for the group (the AND matters — Heavy Mace's E
has low damage but silences the enemy group). Primarily single-target
abilities (Dagger Pair, 1H Fire) are strongest at 3, good/okay at 5, then
fall off hard. Gradings that calibrated the constants: Energy Shaper stays
(very high damage, great in large fights), Greataxe stays (its E hits
everyone in its vicinity), Twin Slayers left to the crystal cost gate,
Primal Staff commentary only.

**Changes.**

- C7-A resolved — a curation gap, killboard corroborated: Falcon Smash
  "deals physical damage within a 4 radius upon impact" (264 vs players);
  the sheet had never scored the E's damage, and the single-scale verdict
  cascaded from that gap. Sheet fix (burst_aoe 4, evidence DIVEPUNCH_RISE)
  → group-scale, brawl-fits, back in every group pool. The loop worked end
  to end: killboard proposed, dumps verified, sheet cited.
- Prong-1 fact overrides (style_overrides.yaml, all cited): Warbow
  (single-target, easily blocked — Magic Arrow's 3.0×2.5 line footprint),
  1H Fire (Pyroblast detonates on the first enemy hit), Hellspawn (the Imp
  transformation, single target, not good above 5). All three: gang
  situational / group unfit / trio fits.
- Prong 2 derived (`weak_group_e` in build_dataset): group-scale dps E with
  damage < 4 sheet points AND no single E tool at 4+ (UTILITY_EXEMPT set +
  interrupt; self-only mobility/disengage/self-sustain never rescue) → the
  trio-class ladder. Catches Phantom Twinblade and Icicle Staff (the
  scattered-2s class; Double Bladed Staff itself is rescued by its engage
  tool); demotion is situational, never unfit, so manual picks stay
  legitimate. Constants PROVISIONAL in the audit report.
- Golden T31/T31b pin the contract (54/54); forge 21/21, parity 60/60, V4
  role gate 78% PASS unchanged.
- C7-B Spirithunter — PROMOTED for clap and clap_kite (a CLAP piece, not
  the bomb-squad archetype): its E carries a massive pierce that enables the
  whole dps line. Dump text agrees (Corrupting Steel pierces through ALL
  enemies, cuts the damage resists of every enemy hit, the trail cuts magic
  resist further). Cited override: clap + clap_kite gang/group → fits;
  brawl/kite keep the situational grade. T23e updated, T31c pins pool
  membership.
- C7-C Bloodletter — no gate change: killboard prominence is a
  MOUNT-CARRIER artifact — battlemount pilots hold it for its mobility, so
  it rides into cohort baskets without being a comp slot. Caveat added to
  KILLBOARD_AFFINITY.md.
- C7-D Galatine Pair — no change: a solo-bomb specialist the comp-fitted
  templates do not demand; the forge passing on it is correct ranking. Stays
  pooled for manual use.
- Commentary logged, not gated: Double Bladed Staff (less utility than a
  mace), Rift Glaive's kidnap fling (rare), Great Cursed vs the utility-E
  curses (feeds the round-6 question). C7-E watch items stand.

### 2026-08-24 — First full E-audit

**Context.** Every weapon's E read against its sheet bundle and derived
verdicts: description damage numbers vs scored damage caps, radius/area/
pierce wording vs damage scale, effect-layer candidates vs curation. 17
structural flags of 137; 13 resolved on inspection (shield-absorb regex
false positives; healers/frontline/support whose incidental E damage
rightly goes unscored — 1H Mace's 203-damage stun leap, Hand of Justice,
Bedrock, Tombhammer, Chillhowl the viability-excluded case; Battleaxe
already decided; Bear Paws' cone damage already scored; crystal weapons
cost-gated regardless). Two Battle-Bracers-class gaps fixed (sheet
curation, dump-cited): Fists of Avalon — Purifying Fist "deals 232 physical
damage ... purges all buffs from all enemies hit", area damage never scored
→ burst_aoe 4 (the sibling Battle Bracers 264/r4 = 4 calibration), now
group-scale, brawl-fits, in group pools; Trinity Spear — "Roots all enemies
in a 3.0 radius on impact ... deals 191.65 physical damage" + the ally
attack-speed mist, impact damage unscored → burst_aoe 2, now group-scale.

**Decisions.** Fists of Avalon purge 3 → 4 (MASTERSHEET tune:sheets); the
dive purge-bomb stands in group pools. Trinity Spear OUT of large-scale
generation: nobody auto-attacks in big fights; its only large-scale use is a
swap for castle gates and territory walls, never as a main weapon. Cited
override: group unfit, every style; gang keeps the derived verdict.
Principle (standing rule 13): an AUTO-ATTACK steroid is not large-scale
utility — ally attack-speed buffs should not carry group slots. The audit
judgment was wrong precisely where derived facts cannot see what players do
with auto-attacks — the case the review step exists for.

**Changes.** Golden T31d pins both; 56/56 golden.

### 2026-08-25 — Round 8: non-stacking slots are earned

**Context (E-first facts).** Rotcaller's E prevents healing for 2 s — great
for clap and clap-kite, whose objective is to hit quick and kill fast;
Damnation adds a massive pierce on a huge area; Lifecurse purges enemy
buffs, a good all-round weapon. Observed bound: in parties above 15 the only
curses seen are Lifecurse, Damnation and Rotcaller; Demonic Staff is not a
true brawl weapon above 7.

**Changes** (structural, no hand list):

- The rule: a member of a derived NON-STACKING group (membership from
  `evidence_spells` — the pool a verified count-once record prices; today
  only `curse_pressure`) earns a GROUP-band slot with an E enemy-DEBUFF tool
  at the standing tool bar (`E_DEBUFF_CAPS`: purge / resist_shred /
  heal_reduction / max_health_cut / damage_debuff, ≥ `E_UTILITY_TOOL_MIN`).
  Members that fail demote to situational at group for every style — never
  unfit, trio/gang untouched, `style_overrides.yaml` still wins. The
  derivation reproduces the observed list exactly: Damnation (pierce 4) /
  Lifecurse (purge 4) / Rotcaller (heal-cut 4) earn; Great Cursed
  (HoT-purge 2), Cursed Skull, Shadowcaller, 1H Cursed and Demonic fail —
  the fear is DISPLACEMENT, not a debuff, which matches the Demonic verdict.
  Fields `e_debuff_max` / `nonstack_member` audit it in
  `out/style_fit_report.json`.
- The gate (both ports): the generation-fit gate also checks
  non-stacking-group members at group band — declared style needs "fits",
  balanced needs fits-somewhere; manual picks score normally. Castle 25
  brawl now fields Damnation + Lifecurse (was Damnation + Cursed Skull, the
  round-6 trigger). Rotcaller composes with the crystal cost gate.
- Band seam, decided by precedent: "bigger than 15" was the phrasing;
  verdict bands split at group 10+. Per the band-consistency decision
  (round 2) the demotion applies at the group band — stricter than literal
  at 10–15. Confirmed in round 9 (15 was a general number). Overrule with a
  cited override if damage-E curses should stay generable at 10–15.
- Irreducible saturation filler, found and pinned: barring the damage-E
  curses changed the beam path at two size-11 matrix cells (castle/brawl,
  faction_war/brawl_clap): the forge converges to an assembly whose last
  slot is slightly negative (−0.02 / −0.07) with NO better legal candidate
  (the fielded member IS the best remaining pick; even the barred curses
  would score negative there). The pre-decision assemblies stay reachable
  manually. F5's contract was strengthened: filler is legal ONLY when
  irreducible — any filler slot with a strictly better legal replacement
  still fails.
- Magnitude-queue note: the damage ordering within the earned trio
  (Damnation least, Lifecurse a little more, Rotcaller most) is in tension
  with the Damnation sheet's DoT-delivered `burst_aoe 6` (the line's
  highest); queued for the magnitude audit (resolved in round 9).

**Evidence.** F19 pins the whole contract (derivation split, pool membership
at 20 balanced + brawl, gang and manual intact, castle-25 forge). 22/22
forge, 56/56 golden, 60/60 parity + embed, tier2 role 78% PASS.

### 2026-08-25 — C7-E: the engine darlings stand; one number gap fixed

**Decision.** The weapons the engine favours are not overrated provided the
actual numbers (damage, support, defence) check out; Great Holy possibly a
little. Number check against the dumps, all seven:

- Evensong — FIXED (the round-1 watch item resolves as an OVER-score, not
  undervaluation). The E deals 112 in r5 — below the E-damage ladder's
  2-anchor (Trinity Spear 191/r3 = 2; Bracers 264 / Fists 232 = 4) — yet
  was scored burst_aoe 4. Trimmed to 2, cited. The real identity stands:
  Dark Auras (−15% healing received / −15% damage dealt / 15% slow, 5r
  around 3 enemies, 5 s) stack up to 3 times — overlapping auras reach −45%
  on a clump; heal_reduction 4 + damage_debuff 2 stand on that fact.
  Killboard absence and this trim tell one story.
- Incubus Mace — grounded, stands: the E is a triple 40% debuff (max AND
  current health cut, damage dealt, healing cast) for 8 s in a cone:
  max_health_cut 4 / heal_reduction 4 / damage_debuff 4 check. Kill-event
  support-undercount explains the absence.
- Great Holy — numbers check (channel 50 HP / 0.5 s to 10 allies ≈ 1000
  HP/s of group healing, +16% ally resists, 10 m CC-resist-ignoring shove):
  heal_sustain 6 / peel 4 / buff_allies 4 are honest. "A bit overrated" has
  no factual home in the sheet — ranking commentary only.
- Hoarfrost (every score adjudicated 2026-08-21, dump-matched), Glacial (15
  s storm = zone 6, low tick = burst 2), Grailseeker (3.5 s recastable root
  wall = root/zone/peel/catch 4, 125 line = burst 2) — consistent, stand.
- Clarent Blade — magnitude note: the E's 170 damage pierces THROUGH all
  enemies (line delivery) but is bucketed burst_st 4, which the size physics
  taxes at scale; if anything undersold at 20. Queued (rebucketed in round
  9).

**Evidence.** 56/56 golden, 22/22 forge, 60/60 parity + embed, tier2 78%.
The C7-E watch list is CLOSED.

### 2026-08-25 — Q16 signed off: the size-physics tables

The current wiki Resilience and AoE Escalation pages match the wired
`mechanics.yaml` tables exactly (single-target damage is punished in large
groups, AoE rewarded), and both shipped `size_physics` tables are confirmed
as presented: `st_value_mult` (25% ST value at 20-man, 20% at 30+) and
`count_mult` (×1.6 clump at 20, ×2.0 at 40+). The tables lose their
PROVISIONAL label; MECHANICS_TODO Q16 is closed. Remaining mechanics number
needing a decision: Q14 (per-style clump/focus counts).

### 2026-08-25 — Round 9: eleven questions, eleven decisions

The audit queue and the mechanics chapter close together.

- Q14 CLOSED with an ordering rule: no numbers available for per-style
  clump/focus counts; for clap, kite, clap_kite and brawl_clap the number is
  usually higher than brawl. styles.yaml values already satisfy it (brawl 3
  the floor); comment updated. A dive/assassination style: only above 20
  people — deferred, 20+-only if ever built.
- Resilience Penetration WIRED: single target is a non-pick at 20+ (too many
  enemy defensives protect that one person); wired as a partial rebate. The
  full 69-row wiki table (post-Realm-Divided, revision oldid=84609,
  retrieved same day) landed in `pipeline/resilience_penetration.yaml`
  (cited; melee-only stat, ranged/magic categorically 0); `build_dataset`
  stamps `resil_pen` (69/69 matched by display name); both ports rebate
  burst_st/execute SUPPLY by (1 − DR·(1−pen)) / (1 − DR) at the style's
  grown focus count — dagger-class 40% pen keeps ~35% more ST at 20-man
  physics, on top of (never instead of) the confirmed st_value weight tax.
  F20 pins stamping, ordering (dagger > mace > pen-less) and
  scale-monotonicity; F1's exact-marginal invariant and 60/60 parity hold.
- RULE queue ADJUDICATED wholesale: all twelve specialist E-supplement
  upgrades stand (list in MECHANICS_TODO). Both three-level ladders
  confirmed (frost zones 6). ONE reversal: Rotcaller's 1H damage-budget
  discount REJECTED — one-handed weapons have smaller base damage, but
  damage comes from equipment and the free hand admits an offhand that can
  INCREASE it — sustained_dps/CURSEDOT 2 → 4 (line default). Principle
  (standing rule 12): no automatic 1H damage discounts; the offhand
  compensates.
- Peel ranking commentary (logged, not gated): on Quarterstaff's
  Separating-Slam 6, Grailseeker or Soulscythe (the tornado knock-up, peel 4
  with Grailseeker) may be the better peelers. Hedged; no score change.
- Damnation damage RESOLVED BY NUMBERS: the weapon is fielded for its
  massive AoE pierce, so the numbers decide the damage score. Dumps:
  Cataclysm deals 24/tick × 6 = 144 total over ~6 s on a 13 m radius — below
  the 191 = 2 ladder anchor. burst_aoe 6 → 2, cited; the r13 blanket +
  escalation stay in cap_delivery; the pierce (resist_shred 4, the weapon's
  identity and its earned-slot ticket) untouched.
- Heal-cut ladder reviewed roster-wide: Dawnsong already accounted
  (heal_reduction 4, −40% inside the fire — the −40/−50% one-shot class:
  Carrioncaller, Incubus, Staff of Balance, Dawnsong all hold 4 beside
  Rotcaller's 100%-negate 4); the bleed/HoT-purge class holds 2. One change:
  Evensong's heal cut is weaker than Rotcaller's — heal_reduction 4 → 3.
- Evensong structural consequence: with both trims (damage 4 → 2 from the
  C7-E check, heal-cut 4 → 3 here) Evensong derives `weak_group_e` (E damage
  2 < 4 AND best E tool 3 < 4) — trio-class, situational at gang/group, out
  of default generation above 3. The round-7 prong-2 rule doing exactly
  what it says; the killboard (0–1 observed) corroborates. A cited override
  can rescue it if it should be generable.
- Clarent Blade REBUCKETED: the E is AoE damage — burst_st 4 → burst_aoe 4
  (the 170 wave pierces through all enemies; line-delivered area). It now
  escalates instead of being ST-taxed.
- Curse band seam CONFIRMED: "15" was a general number; the round-8
  group-band cutoff at 10+ stands, no seam.

**Evidence.** 23/23 forge (F20 new), 56/56 golden, 60/60 parity + embed,
tier2 role 78% PASS, builds/interactions/provenance/patch/cohort/codec/
display green. With Q1/Q3/Q4/Q6–Q10/Q12/Q14–Q16/Q19 closed, the remaining
mechanics backlog: per-spell burst_aoe escalation gating (needs a styled
validation round), Q11/Q13 (asymmetric numbers — parked by design), Q17
(meta prior — parked behind win-lift), the PASV/TOP magnitude review boards.

### 2026-08-25 — Round 10: the role layer

**Context** (both verified before design): (1) the kit advisor gave the
whole brawl-20 comp the same chest (Hellion class) regardless of job —
comp-pool marginal valuation makes item values wearer-independent
(identical to four decimals across members) once the tank pool saturates;
(2) Grailseeker sat in a dps slot (`role_hint: melee` → dps) despite 4
damage vs 18 utility points and a 125-damage E.

**Decision (the model, recorded in roles-design.md).** A role is a property
of the member-in-comp; weapons carry MENUS of roles selected by gear (one
Realmbreaker can play different roles; roles are not locked 1:1; Royal Armor
= team energy / Royal Jacket = team cooldowns / Hellion = damage — all three
wiki-verified); roles are the PRIMARY objects, populated from evidence, not
137 hand menus; coarse classes stay as the band layer; cross-class
assignment is allowed and highlighted; advisory is a headline feature (the
Longbow-in-the-wrong-jacket flag; 3 heavy maces and 0 engage tanks is an
obvious flag); scoring stays capability-driven, roles never add points.
Correction landed the same round: Incubus's doctrine kit is Sacred Ground +
SNARE CHARGE, not Guard Rune (roles.yaml doctrine field).

**Changes (increment 1).** `pipeline/roles.yaml` — 18 roles (engage /
stopper / off tank; main / kite / brawl healer; aura / shield / curse / zone
support; ranged_aoe / sustained_brawler / bomb / pierce_enabler /
dive_cleanup; meta battlemount / caller / scout, never forged), 69 weapons
on menus, EVERY membership cited (comp slot labels — which already field
Lifecurse as dps AND support, Camlann as support AND tank; the 2026-08-25
role-taxonomy research with the 18-slot "Realmbreaker Support" comp and the
Earthrune-as-engage-tank case; derived signatures like clump_core). The
build validates fail-closed and stamps `role_menu` (inverse index);
`out/roles_report.json` is the grading board. Both ports carry
`detect_role` (played role from weapon × worn chest class) and
`role_advisory` (off_role_kit per member; no_engage_tank at 10+ with 2+
frontliners and no clump maker) — DESCRIPTIVE only; R5 pins comp_score
untouched; parity carries role_advisory per case (60/60). The status card
renders the role tally and flags from the members' LOADOUT chests.
tests/test_roles.py R1–R6 (6/6): R6 is the original bug pinned — Incubus +
Grailseeker in Hellion Jacket both flag off-role kit. Pending increments:
kit-doctrine advisor (2), forge role assignment + graded need profiles (3),
uptime economics (4).

### 2026-08-25 — Grading pass 1: the taxonomy is FUNCTIONS beside SEATS

**Context.** Curse support should not be its own role — those weapons belong
in pierce / purge / heal-cut categories; aura support should split into the
individual aura effects (Demon Armor, Judicator Armor, Guardian Armor
auras).

**Changes (taxonomy v2).** `curse_support` / `pierce_enabler` /
`aura_support` dissolved — SEAT roles (uniformed) vs FUNCTION roles (`pierce`
= Damnation + Spirithunter; `purge` = Lifecurse + Fists of Avalon;
`anti_heal` = the round-9 heal-cut roster: Rotcaller, Carrioncaller,
Incubus, Dawnsong, Staff of Balance, Evensong) that ride along with any seat
and claim no chest; plus a typed `gear_effects` catalog (energy font /
cooldown banner / enemy-weaken aura / ally force shield / reflect area /
lifesteal steroid — each cited to its catalog item or the wiki, with
evidenced carrier weapons). Detection reports seat + functions + carrying;
kits are judged against SEATS only — the split is what keeps R6 honest (the
anti-heal function never excuses Incubus's damage jacket). R1–R7 7/7, 60/60
parity with functions + carrying per case.

### 2026-08-25 — Grading pass 2: the E-first tiered sweep

**Context.** Every weapon belongs on the roles its abilities support
(Carving Sword was absent from pierce despite a pierce AoE on its E);
abilities on Q or W (axe heal cut, curse pierce/snare Ws) are a secondary
level; a weapon's primary roles come from its E. Verified first: Carving's E
(dash) cuts damage resistances up to 20% through the line — the sheet
already scores resist_shred 4; the book had never swept it.

**Changes** (derivation, not hand lists): function-role membership sweeps
ALL weapons from the sheets' own slot structure — E-slot cap ≥ 3 → PRIMARY;
a Q/W ABILITY ≥ 2 → SECONDARY (passive procs and always-on stats excluded:
the rule names abilities; Longbow's shred proc dropped out on that
distinction). Results: pierce 5 primary (Damnation, Spirithunter, Carving,
Crossbow, Dreadstorm) + 77 secondary — shred Q/Ws are near-universal (Iron
Breaker, Sunder Armor, Frazzle, Piercing Arrow, Frost Lance, the curse Armor
Piercer); kept at the standard bar because the reference example lives at
rung 2, with a per-role `secondary_min` knob recorded. purge 5+10, anti_heal
6+17 (the axe-bleed class). Weapons carry `role_menu` (primary) +
`role_menu_secondary`; detection/advisory/parity carry the `secondary` list;
the status card shows secondary-function chips dimmed. R8 pins the cases
(Carving E-primary; axe bleed + smash secondary; curse Armor Piercer
secondary with Damnation staying E-primary). 8/8 roles.

### 2026-08-25 — Offhand-pairing check

Question: whether Incubus is mostly paired with Leering Cane for its +CC
duration. Verified online, an honest split: Leering Cane + Incubus IS
documented (albionfreemarket "Incubus Tank"; grind duo-gank) and the broader
mace-CC + Leering pattern is confirmed meta (metabattle's 1H Mace ZvZ lists
Leering Cane as PRIMARY offhand; CC-duration stacking threads); the
earlier-cited "Incubus ZvZ Support" with Timelocked Grimoire re-verified
item by item (real page, 2026-02); Incubus-in-ZvZ evidence splits Astral
Aegis / Timelocked / Leering. Usage-stat sites (murderledger) unreachable
(403). Both pairings recorded in the gear sheet comments; the
CC-stacks-with-CC pairing rule queued as increment-2 kit-advisor logic.

### 2026-08-25 — Increment 2: the kit-doctrine advisor

**Decisions.** Doctrine covers the whole build — food, potion and capes
included; passive defaults per class are real (cloth = damage passive,
leather = CDR, plate = CC duration on the frontline).

**Changes** (derivation end to end): (1) UNIFORM GATE — `kit_options` (both
ports) resolves the weapon's primary seat (`role="auto"`; None = the old
ungated pool) and hard-gates the CHEST pool to the seat's uniform classes —
R12 pins Incubus + Grailseeker to plate-only chests; the everyone-gets-
Hellion bug is dead at the pool level while manual builds still score
anything. (2) DOCTRINE TIERS — every other slot (head / shoes / cape /
offhand / food / potion) carries a doctrine tier MINED from the seat
members' observed reference builds (builds_index, conservative id
normalisation, build-id citations; audit board = roles_report
`kit_doctrine`; off-uniform sightings reported, never admitted). Mined pools
are real doctrine: stopper chests Demon/Judicator plate, brawler chests
Hellion + ROYAL JACKET (the Realmbreaker variant, surfacing cited with its
`carries: cooldown_banner` chip — R16), Beef Stew on damage seats vs
Omelette on tank/healer seats. Ordering (from T22): context-free kits rank
the doctrine tier first; COMP-AWARE kits rank the exact marginal first (the
engine's own physics outranks a sparse observation — the initially shipped
tier-first comp ordering handed the castle-24 tank an Assassin Hood and
broke the T22 pin; doctrine demoted to annotation + tie-break in that mode).
(3) PASSIVE DOCTRINE — roles.yaml names only the FAMILY per gear class; each
piece's actual passive id resolves from its own dumps menu and the magnitude
parses from the spell description (Aggression +8% damage & healing cast;
Quick Thinker +5% CDR display-only — no invented channel; Authority +10% CC
duration; Tenacity +20% CCR), stamped `doctrine_passives` per piece per seat
class and fed into the build stat channels by `build_extra(role=)` — R14.
(4) EMERGENT PAIRING — `bonusccdurationvsplayers` joined the dataset stat
copy and the new `cc_mult_caps` channel (stun/root/slow/silence; knockbacks
are instantaneous and stay out) multiplies the wearer's OWN CC — R13 pins
Leering Cane worth something on Incubus and exactly nothing on Great Fire:
the pairing as physics with zero hand lists. Kit parity rides the 60-case
suite (comp-aware + context-free serialised per sampled case, 1e-9); the JS
port's missing brawl-cloth-gate drift was found and closed in the same
mirror. R12–R16 (16/16), 56/56 golden, 23/23 forge, 60/60 parity.

### 2026-08-25 — Grading passes 3–7: shield break, the equipment model, passives, directionality, offhands

**Third pass — SHIELD BREAK split out of purge.** Hammers only break
shields on Q, which is not a purge; shield break is Black Monk's primary
role. The sheets' own row comments already carried the distinction (the
2026-08-21 shield-break-below-true-purge placement). Shipped as the
SPELL-CLASSIFIED split: a function role may claim specific evidence spells
(`spells:` on the role record — Iron Breaker, Black Monk's E, Claws' E)
which the sweep routes to IT and never to the unfiltered role on the same
cap; `primary_min: 2` on shield_break because its ladder is deliberately
held below the purge ladder. Result: purge = TRUE purges only (Lifecurse,
Fists, Heavy Mace's Battle Howl, 1H Arcane, Daybreaker — zero secondary);
shield_break = Black Monk + Claws E-primary, the eight hammers Q-secondary.
R9 pins it; 9/9 roles, 60/60 parity. Next: the equipment-role pass folds
into increment 2 — classify all 41 catalog items (and missing uniforms like
Royal Armor) into role uniforms + gear effects, then rebuild the kit advisor
on top.

**Fourth pass — the EQUIPMENT model.** Gear mirrors weapons: three ability
choices, the tree shares the first two, the identity of a piece is its
unique spell; damage and tankiness follow the tree and are classified from
the numbers. Numbers pulled and verified: cloth chests 54–68 armor / +40–50%
damage-heal, leather 92–100 / +25–30%, plate 152–161 / +0–5% — the
numbers-derived class agrees with the tree id on every statted chest.
`classify_gear` stamps `gear_class` (numbers first) + `role_affinity` per
worn item; the items board + gear-effect CANDIDATES (ally-buff actives not
yet catalogued: Lymhurst Cape, Druid Cowl, Knight Helmet, Guardian Helmet,
Mistcaller) ship in out/roles_report.json for grading. Recorded gaps: stats
bank zeros for head/shoes/offhand (class falls back to tree id); gear
records curate only the unique active — the tree-shared first two abilities
await curation for full kit picks. R10; 10/10 roles.

**Fifth pass — the law renamed, passives counted.** The principle is
UNIQUE-ABILITY-FIRST (armor uniques sit on d/f/r, not E), with E-first as
its weapon case (roles-design.md). Tree passives are unique to the tree and
most cloth users take the damage passive: the tree passive sets (already in
gear_spells.json) now stamp every worn item (`tree_passives`, on the items
board): cloth = +8% damage/heal OR −10% cast time OR energy cost; leather =
balance / AA speed / CD reduction; plate = MR-AR / CC duration / CCR /
threat. The passive PICK per role doctrine (cloth dps takes the damage
passive) recorded as increment-2 work. R10 extended; 10/10 roles.

**Sixth pass — Mistcaller/Lymhurst directionality fix.** Mistcaller and
Lymhurst capes are self buffs, not ally buffs. Dumps agree: Mistcaller has
NO active (a self stat stick; the offhand stats-bank gap had left its
identity to a curated proxy) and Lymhurst's ability is
PASSIVE_CAPE_LYMHURST, a self proc — both `buff_allies 2` rows were
GEAR_STATS-grounded direction errors (the V5 catch-#1 class). Rows removed
with citations; both items drop off the gear-effect candidates list (now:
Druid Cowl, Knight Helmet, Guardian Helmet — all genuinely ally-targeted
actives). 10/10 roles.

**Seventh pass — OFFHANDS classified by their numbers.** Offhands have no
active ability; their usefulness comes from stats, so the stats classify
them. Found: the stats bank (`out/item_stats.json`) held every offhand's
profile all along — the gap was the dataset build's copy filter carrying
only the eight chest-centric fields. Widened (defense bonus, threat,
cooldown/cast-time reduction, energy, HP, heal modifier — engine scoring
channels unchanged) and offhand `role_affinity` now derives from the STAT
PROFILE: defense/threat → tank seats (Shield / Caitiff / Sarcophagus),
heal + damage output → healer + dps seats (Blueflame Torch the hybrid),
cooldown/cast/energy utility → caster-support families (Mistcaller = pure
CDR 0.96%/100IP → healer/support seats, matching its comp-cited seat beside
the 1H Mace engage tank; Astral Aegis = defense + energy discount → the
widest board, tank AND support seats). R11 pins Mistcaller + Sarcophagus;
11/11 roles.

**Seventh pass addendum — the FULL offhand roster.** The catalog held 6 of
the bank's 18. All 12 missing offhands added (empty capabilities BY DESIGN —
no active; the stat profile is the identity): Tome of Spells (cast time),
Celestial Censer (+heal, −defense), Muisak (+damage +heal, energy tax),
Leering Cane (+CC duration), Cryptcandle (+spell damage, −defense), Eye of
Secrets / Sacred Scepter / Taproot (the BOOK CLASS — near-no modifiers,
identity = raw item-power progression, 725/800/775 vs 700 baseline at T4;
affinity deliberately empty pending grading), Unbreakable Ward +
Facebreaker (shields: defense + threat; Facebreaker adds damage), Timelocked
Grimoire (cast + CDR — the guide-cited Incubus-support pairing), Torch
(attack speed + CDR). One sign bug caught by the board itself:
Censer/Cryptcandle carry defense PENALTIES and had landed in tank seats —
the defense rule now requires a POSITIVE modifier (Censer → healer seats
only, Cryptcandle → dps seats only). 11/11 roles.

### 2026-08-26 — Full-board grading: 465 rows reviewed, 15 decisions

**Context.** The complete roles_report (seat memberships, kit-doctrine
pools, gear effects, the 43-item equipment board) graded as an interactive
board; everything not listed below accepted as shipped.

**Changes.**

- Eight MEMBERSHIP corrections (roles.yaml, each cited at the removal site):
  Iron-clad off stopper_tank (consistent with its standing ≥ 10 exclusion);
  Great Holy off main_healer (a brawl anchor — the brawl_healer seat stands);
  Witchwork off shield_support (a clump and damage weapon, not a cleanse
  weapon — engage_tank stands); Black Monk off shield_support (it purges
  enemy shields rather than shielding the team — shield_break stays
  primary); Chillhowl (not good for group content), Glacial (more DPS than
  support) and Permafrost (dps and engage dps) off zone_support — the frost
  dps pair keep their ranged_aoe seats, Chillhowl leaves every menu; Great
  Arcane off bomb_aoe (its role is to stop enemies and set them up for the
  one-shot dps — shield_support stands as the setup seat).
- Five KIT-DOCTRINE decisions, shipped as the new cited
  `kit_doctrine.overrides` layer (a drop must name a mined item, an add a
  cataloged uniform-legal one — anything stale blocks the release; audit
  trail in roles_report `overrides`): engage potion drops Resistance Potion;
  stopper cape drops Lymhurst; stopper offhand swaps Timelocked Grimoire →
  Leering Cane; brawl_healer offhand swaps Sarcophagus → Blueflame Torch
  (conditional on the healer wearing Judicator Armor — conditionality
  recorded; conditional kit logic stays increment-3+ work).
- One EQUIPMENT decision via the new `gear_affinity_overrides` layer:
  Leering Cane's derived caster-support affinity (its small CDR stat)
  replaced with [stopper_tank] — it is for one-handed stop tanks, not brawl
  healers.
- One VIABILITY decision: Dagger Pair + Deathgivers excluded at ≥ 7 (never
  fielded in medium-large scale) — composition.yaml exclusion with the
  standard evidence-gate lift; trio stays open, the 4–6 band was already
  gated by the round-7 style rules, their dive_cleanup seats stand at small
  scale.

**Evidence.** R17 pins the whole batch (17/17 roles); 56/56 golden, 23/23
forge, 60/60 parity.

### 2026-08-26 — Increment 2.5: per-weapon doctrine and effect quotas

**Context.** Hand of Justice in Demon Armor is unlikely; the one observed
counter-case has to be explained rather than admitted — it may be filling a
role, the comp may have lacked Demon Armors so the engage tank took one, or
four cases is too small a sample. Hypothesis VERIFIED before wiring: the
sighting is cb_clonepeek_zvz20 seat 19, and that roster fields 4 Demon + 4
Judicator + 1 Guardian — a reflect-heavy frontline allocating its quota.

**Changes** (both ports, R18): kit pools also mine per weapon (`kit_weapon`
/ report `by_weapon`; Polehammer's own tier is Knight ×5 of 5) and the
advisor ranks the weapon's own observed tier first with sample-size honesty
(`doctrine_n`); effect-carrier chests (the typed gear_effects) are excluded
from weapon tiers — they stay in the seat pool tagged `effect:` and are
quota-mined per near-complete roster (`effect_quotas`: reflect shells in 7
of 8 rosters, typically 3 copies — DISPLAY-ONLY until the pattern is
graded); overrides gained weapon scope and seat drops cascade so a
seat-wide decision cannot resurface through a weapon tier. 18/18 roles,
56/56 golden, 60/60 parity + embed.

### 2026-08-26 — Sample widened (MetaBattle adapter v2)

The MetaBattle adapter captures every group-PvP build category (ZvZ +
Hellgate 5v5/10v10 + Crystal League/Arena + Ganking — solo/PvE modes out),
`content` derives from each page's own mode category (MODE_CONTENT
priority), and the batch ships as `data/published_builds/metabattle.yaml`
(metabattle_zvz.yaml retired; the ZvZ style-fit cross-check filters
`content == "zvz"` so ganking builds cannot vouch for group-scale fitness).
46 pages captured (was 31), evidence corpus 222 → 237 records over 65 → 74
weapons, 914 kit-doctrine observations, new `ganking_smallscale` + wider
`hellgate_5v5` buckets — dive_cleanup's daggers carry their own observed
kits. Two records quarantined on an ambiguous 'Frost' spell name (fail
closed, correctly). 53/53 builds. More caller sheets in
data/published_comps remain the highest-value growth per observation.

### 2026-08-26 — Increment 3: need profiles

**Context.** Mix calls collected before any evidence was shown (20 brawl:
one or two ways to clump, focus on 4 stop tanks, 3 supports who slow or
deny, 3-4 heals, the rest melee dps, heal cut and pierces covered; clap-kite
the same skeleton ranged; territory: more stop tanks; 7-man shapes) — with
the directive that the data, not belief, sets the numbers. Evidence pass:
the forge's own mixes at five matched cases, the 8 curated rosters,
Wardergrip's guide (4-5 tanks, 3-4 supports, 4 healers), and a new
killboard roster miner (`pipeline/sample_rosters.py`, sanctioned albionbb
endpoint → `out/roster_mixes.json`): 139 near-complete fight rosters (24
party / 39 mid / 76 gang; wiped sides so the whole roster is attributed —
the winner cohort reported separately because healers under-attribute on
winning sides). Findings: healers 3-4, mode 4 (every source agrees); ENGAGE >
STOPPER everywhere (live 1.95/1.09, curated 2.6/1.8) — the data overruled
the 4-stopper call, which survives as the territory-defense override;
pierce fielded by 100% and heal-cut by 92% of live party rosters; shield
support 1.5 live vs 3.2 curated (flank-party skew confirmed); dive_cleanup
lives at gang scale.

**Changes.** roles.yaml `need_profiles` (engage 2-3 / stopper 1-2; territory
stopper 2-4 engage 1-3; off ≤ 1; shield 1-3; zone ≤ 1; pierce ≥ 1;
anti_heal ≥ 1; min_size 15, scaled by size/20 half-up), validated
fail-closed in build_dataset, shipped in the dataset, enforced in BOTH ports
as generation constraints riding the forge's predicate channel (seat maxima
via ctx seat_max; locked members count; manual parties always score; below
15 nothing arms). Both ports produce identical profile-constrained 20-man
forges (checked at three armed cases; the 60-case parity forge runs at size
8, below arming — recorded gap). F21 pins the contract; 24/24 forge. The
forge's clap_kite melee-heavy failure case is the motivating defect; its
full fix (delivery-mix bands per style) awaits a numeric decision on the
clap_kite ranged-core minimum.

### 2026-08-26 — Conditional-payload rule

**Context.** The new comp-status radar showed a forged 20-man clap as "split
identity — melee and ranged damage pull apart", every true-ranged staff
flagged as pulling against a MELEE core. Diagnosis: the forge satisfied the
7-strong ranged-AoE core largely with FLEX bruisers (fists/sword class — the
predicate is honest, their Es land at range), and comp_identity counts flex
damage on its home melee side, so the roster read 59% melee. A
delivery-category rule (a weapon must be ranged or melee) was REJECTED in
favour of job difficulty: clap gives up leather tankiness for naturally high
damage delivered almost instantly — Permafrost delivers an AoE stun
instantly, Spiked Gauntlets similarly; the job is much harder for Clarent
Blade, which has to stack its Q. Per-weapon reads: Clarent/Ursine are nice
brawl weapons that fit clap poorly; Carving is good brawl for resistance
shred; Longbow KEEPS clap (damage over walls); Earthrune (a very meta clump
tank), Malevolent Locus (good support), Enigmatic/Lifecurse/Blight (fine
supportive weapons) untouched; Energy Shaper keeps (good damage); Morning
Star unused — killboard: 1 large-fight appearance, none mid/small.

**Changes** (fully derived, zero hand lists): parse_dumps gains a structural
`channel` fact (a `channelingspell` node anywhere in the spell tree —
Gravitas hides its channel in a sub-spell, so the walk follows references);
derive_style_fit demotes a GROUP damage carrier to situational at
clap/clap_kite gang + group when every damage E is ramp-dependent (consumes
charges/stacks — description-detected, the charge spend hides in a generic
removeactivespell) or a non-ranged channel. The scoping needed no
exceptions: every "keep" falls out of existing derived facts (ranged
channels exempt by delivery; support/tank seats exempt by damage_scale
none). Catalog diff: exactly 4 weapons flipped — Clarent, Carving, Ursine,
Rift Glaive (a true positive: its E deals damage based on Spirit Spear
Charges; crystal is cost-gated below 30 anyway). Result: the same clap forge
fields Damnation + Heavy Crossbow + Wailing Bow + 2× Permafrost instead of
Ursine/Clarent, reads "Clap-Kite · leaning" with ZERO conflicts at 42% melee
(a Witchwork-and-Damnation outcome was predicted before the fix ran).
Fitness 139.4 → 139.2: the sacrifice lives in the pool, not the score — a
composition should have some natural things it lacks. Kite deliberately
untouched pending its own decision; the >100% radar readings were explained,
not retuned (targets are comp-fitted floors at 0.9× the least a good comp
fields; any target reshaping waits for its own evidence round). T32 pins
the pool membership both ways; 57/57 golden, 24/24 forge, 60/60 parity +
embed, 18/18 roles, tier2 78%.

### 2026-08-26 — Kite extension

**Context.** The kite-20 forge was challenged next and the engine agreed
(comp_identity: split identity, 65% melee — five sustained melee brawlers
fielded). Three gaps composed: the conditional-payload rule had parked kite,
"flex fits everywhere" admits every bruiser, and kite carried NO ranged-core
constraint (increment 3's ranged_aoe_core went to clap/clap_kite only).
Evidence before numbers: the one real kite 20-man in the corpus
(ss_kite_20, albioncompo) fields 13 ranged / 4 melee / 3 flex — the melee
bodies are all tanks/stoppers plus the pierce bot, ZERO ramp/channel
bruisers, flex damage instant-payload only — and counts 5 ranged-AoE-core
predicate qualifiers (lower than clap's 7 because kite pressure legitimately
includes curse/sustained ranged damage the burst-AoE predicate does not
count).

**Changes.** Conditional-payload extended to kite gang + group (the same 4
weapons flip their kite cells: Clarent, Carving, Ursine, Rift Glaive); kite
constraint_overrides gain ranged_aoe_core min 5 at 20-29 / 4 at 15-19 (0.9×
the observed 5, rounded to the firmer side per the clap-7 precedent).
Result: the kite forge drops from 5 sustained brawlers to 1, fields a real
ranged core (Longbow, Permafrost, Glacial, Dawnsong, Bow of Badon, Damnation
+ the arcane support stack), melee share 65% → 38%, zero conflicts. Honesty
check recorded: Hellfire Hands generates in kite — its E (Boulder Toss) is
genuinely unconditional, so the derived rule correctly passes it; its clap
exclusion is the separate 2026-08-24 override, which is clap-scoped, and
whether Hellfire belongs in kite generation is open. T33 pins pool
membership + the armed core minimum; 58/58 golden, 60/60 parity + embed,
tier2 78%.

## Dressed forge, dressed validation, and the evidence passes (2026-08-27 → 2026-08-29)

### 2026-08-27 — The display ruler

**Decision.** No display reads above 100 for anything, and the 0–100 scale
rests on ground facts. The radar and the capability board had been quoting
supply as % of TARGET — and targets are comp-fitted floors (0.9× the least
any good comp fields), so a competent comp read >100% everywhere and the
playstyle tradeoffs that define a style (a clap comp gives up tankiness on
its dps; a brawl comp gives up some damage) were invisible. 100% = the
comp-fitted soft cap (1.15× the MOST any good comp fields, from the same
2026-08-21 recalibration — ground facts, style-neutral), so a real brawl
ball pushes ~100% Frontline while sitting low on ranged Damage and a clap
reads the opposite. Per-cap supply counts up to its own soft cap, so nothing
reads above 100; stacking beyond the ceiling shows as the purple overstack
marker, never a bigger number. The target minimum stays visible as a brass
TICK per radar spoke and per capability bar. Scoring untouched.

**Changes.** Both surfaces (dl radar, renderGroups bars); parity embed +
display gates green.

### 2026-08-27 — Six capabilities promoted

**Context.** Audit of the taxonomy against the sheets: 31 capabilities
supplied by weapons/gear, only 25 scored by any template. The six orphans
were fully curated with cited evidence AND used by other layers (the kite
chain's Slow stage reads slow + root; Peel reads anti_dive; mechanics.yaml
scales all three geometric CC caps) — but no template carried a requirement
row, so fitness weighted them ZERO: an AoE root earned a weapon nothing.
Evidence per cap: slow 87 weapons / 220 pts, root 74/190,
knockback_displace 56/150, anti_dive 19/62, interrupt 19/40, max_health_cut
5/12.

**Decision.** Fitted from the REAL comps by the standing 2026-08-21
convention (target = 0.9× the least any good comp fields, soft = 1.15× the
most), measured in the same weapon + spell-pick unit as every existing row
(the unit re-base is a separate pending question and must move ALL rows at
once). Weight 1 across the board: the documented low-and-flat start for a
newly promoted capability (design doc; anti_zone/damage_debuff precedent),
because an unvalidated number must never dominate a recommendation —
raising them is a later decision once the effect on real picks is seen.
scales: true for the three geometric CC caps (mechanics.yaml
geometric_caps), false for the dedicated-slot tools. Honest gaps recorded,
not invented: castle and faction_war get NO rows (zero comps in the corpus
for either content); castle_outpost gets no max_health_cut row (no comp
fields it); roads is fitted from ONE comp, so its target is 0.9× a single
observation and its soft cap uses the median soft/target spread of the
multi-comp contents (a 1.15× band around one point would flag overstack
instantly) — re-fit when a second roads comp lands.

**Evidence.** blackzone scores 24 → 30 items, total weight 121.5 → 127.5;
blap measures at or above target on five of the six (anti_dive 0.0/1.2 is a
real, visible hole); the CC-utility weapons the model was blind to (Bedrock
Mace 17 pts of newly scored supply, Incubus 13, Tombhammer 13, Frost
Staff's root 6) now earn what they bring. Full battery green. Still
unpromoted, recorded as open: reveal/anti-stealth and enemy CC-resistance
reduction; PvE-only effects (mob defense/threat/mob CC duration) stay out.

### 2026-08-27 — The last two effects: one added, one refused, one claim retracted

**Context.** An earlier claim held that two effects were real in the dumps
with no home in the taxonomy. Investigating both to the dump level found
that half of that was wrong.

- "Reduce enemy CC resistance" DOES NOT EXIST as a supply — retracted. The
  effect map already carried `crowdcontrolresistance-: enemy: []` with a
  reasoned non-promotion ("1 weapon line"); what the dumps carry is the
  per-spell `@ignorecrowdcontrolresistance` FLAG on 54 equippable CC spells:
  a spell QUALITY (this CC bypasses their resistance), the same shape as
  resil_pen, not a supply a team fields. Recorded in the map: if ever
  modelled it belongs beside resilience penetration as a multiplier on the
  CC caps, never as a capability row.
- `reveal` stays unpromoted, with evidence: every WEAPON source of
  remove:invisibility is a PURGE spell (Battle Howl, Arcane Orb, Cripple,
  Breakthrough, Iron Will, Devastating Strike, Enfeeble Blades: invisibility
  is a buff, so purging strips it), so a reveal row would double-count purge
  on seven lines; only TWO sources reveal WITHOUT purging (Guardian Armor's
  Enfeeble Aura, Stalker Hood's Mortal Agony) and both are GEAR, which the
  effect catalogue did not index (367 entries, weapon spells only) — the
  lint rejected both claims, correctly, and they were backed out rather
  than forced through the override channel. No comp fields anti-stealth
  either.
- A real curation gap, fixed: Defensive Slam was never curated. A Q on five
  maces (Oathkeepers, Morning Star, Heavy Mace, Camlann, 1H Mace) granting
  +0.15 damage resistances AND +0.15 Crowd Control Resistance to the caster
  and up to 10 allies in 8 m for 4 s — the effect map already routed both
  halves (`damageresistance+` / `crowdcontrolresistance+`, ally direction →
  buff_allies, peel) and the spell HAS structured effects, but no sheet ever
  cited it, so five maces scored zero for a genuine group defensive buff.
  Curated buff_allies 3 (calibrated against Royal Armor's 10-ally energy
  buff) + peel 2 (kept below Guard Rune's outright CC immunity at 4 — a
  resistance is not an immunity); both marked for review. Effect:
  Oathkeepers buff_allies 4, Heavy Mace / Camlann / 1H Mace 3.

**Standing gap recorded.** The effect catalogue indexed weapon spells only,
so every gear-sheet claim rested on prose + overrides rather than structured
evidence — the reason the reveal claims could not be checked. Lint PASS.

### 2026-08-27 — The effect catalogue covers gear

**Changes.** `effect_catalogue.py` indexes gear actives AND passives from
gear_spells.json alongside weapon spells (367 weapon + 194 gear = 559
indexed), tracks `gear_lines` / `gear_line_count` per effect beside the
weapon counts, and every gap report (unmapped / no-prose / needs-a-call)
spans both sources.

**Evidence.** The lint went from silently skipping gear to FAILING with 6
grounded errors, each adjudicated against the newly visible structured
effects, none waved through the override channel: (1) Fiend Robe's Fear
Aura `anti_dive` → `knockback_displace` (its only enemy effect is
forced_movement); (2) Fiend Sandals' Position Swap `mobility` DROPPED (its
only effect is invincibility; one repositioning trick is not group
mobility); (3) Hellion Shoes' Mark of Sacrifice `mobility` → `catch` (the
mark strips the target's movement buffs); (4) Specter Shoes' Spectral Run
`mobility` DROPPED (invisibility plus self-penalties, no speed component);
(5) Knight Helmet's Displacement Immunity `buff_allies` DROPPED (the map
routes an ally IMMUNITY to peel, which already carried it); (6) Demon
Armor's `tankiness` was BACKWARDS — Protection of the Fiends grants
magicresistance+ / physicalarmor+ to ALLIES while applying magicresistance-
/ physicalarmor- to the WEARER: the chest buys the group's durability with
its own. Re-cited to buff_allies 3. That error survived every review
precisely because gear claims were unverifiable. Gear sheets: 6 errors + 34
unverifiable warnings → 0 errors, 5 warnings. Lint PASS, dataset rebuilt,
full battery green.

### 2026-08-27 — Gear combat expansion and the T22 re-pin

**Context.** The full combat gear catalog entered the capability sheets: 76
new pieces in `sheets/gear/combat_expansion.yaml` (19 heads, 16 chests, 21
shoes, 12 capes, 8 potions; gatherer/decorative/economy food skipped), every
score citing the item's real ability or GEAR_STATS, with 23
`effect_overrides.yaml` additions for the known parser-artifact classes
(teleports, immunity windows, self-cleanse, direction misreads — each
dumps-cited). The expansion exposed a latent kit-advisor hole: comp-aware
ranking was exact-marginal-first across the WHOLE catalog, and with 27
heads curated the castle-brawl control tank's "best head" became Mercenary
Hood (scarce damage_debuff/interrupt), then Graveguard Helmet (scarce
heal_burst) — golden T22 red.

**Decision.** Kits follow what tanks are observed wearing: Cleric Cowl is
common on tanks, especially in brawl comps; Graveguard Helmet is never worn
on tanks.

**Changes.** (1) MetaBattle re-fetch widened the build sample 180 → 237
records — the observed engage_tank head tier now contains Cleric Cowl
(confirming the claim from evidence) and Graveguard Helmet appears in no
tank tier; (2) comp-aware kit ranking went DOCTRINE-TIER-FIRST in both ports
(marginal picks within the observed tier, never outside it — the
increment-1 "member's job, not comp pool" rule closed for the full catalog);
(3) T22 re-pinned to the mechanism: the tank head must come from the
observed doctrine tier and never the off-tier marginal bait. Heavy Mace's
comp-aware head resolves to its own observed tier (Assassin Hood / Judicator
/ Hellion Hood ×2 observed). Potion rows and the flagged judgment scores
(marked "review" in the sheet) queue for the next validation round.

### 2026-08-27 — Dressed forge and the T30c re-pin

**Context.** forge/recommend now evaluate DRESSED candidates (weapon +
combo + doctrine kit, one divergent variant; spec
`notes/specs/2026-08-27-dressed-forge-design.md`), priced by the exact
comp_score-with-gears the page displays (the page also started passing
LOADOUT gear to scoring the same day — a discovered gap: the engine had
scored full builds since 2026-08-20 but the UI never sent them). One golden
flipped: T30c's "5th Longbow is negative" fixture was a NAKED four-stack,
and the dressed candidate's kit (Knight Helmet / Cleric Robe / Royal Sandals
/ Martlock Cape / Muisak) closes ~28 units of genuinely missing supply →
verdict "ok" +13.3. Against the same four-stack DRESSED in its own kits (the
real-world case) the pick is negative −1.83 with caps_gain 0.0 — the
2026-08-24 shape.

**Decision.** Re-pin with the dressed fixture, and pin the naked-party
behaviour as the model's documented honesty.

**Changes.** Search fixes landed with the change: 1-opt may re-pick the
SAME weapon (the beam freezes combo + kit under an earlier partial state;
re-resolution was unreachable — F5's faction_war case proved it); F5's
reducibility checker prices dressed and legal-only; variant count capped at
2 by measurement (2.8× at 3 variants; 1.6× / 0.9× at 2 — inside the 2×
budget).

### 2026-08-27 — Dressed validation hardening

**Context.** Plan `notes/plans/2026-08-27-dressed-validation-calibration.md`,
findings in `notes/findings/2026-08-27-*.md`. The audit that motivated it:
since the dressed forge, `recommend(party, n)` with no gears prices NAKED
incumbents against DRESSED candidates — so every historical V3/V4 number was
an asymmetric hybrid (neither weapon-model benchmark nor production). All
pre-2026-08-27 numbers are re-labelled "weapon-only-incumbent benchmark with
dressed candidates".

**Changes** (no scoring change anywhere; full battery green and byte-stable
on the legacy metrics):

- `Engine.set_dressing(False)` (both ports, parity case, contracts
  `tests/test_validation_modes.py`): candidates evaluate naked through the
  identity short-circuit — one formula, no second scoring path. Default ON;
  a validation affordance only.
- V3 modes: `score --mode w` (V3-W, symmetric weapon-only) / `--mode d`
  (V3-D, production dressed: incumbents in recorded gear else doctrine v0
  else honestly naked, source recorded; candidates dressed). The 70% gate
  applies to V3-D. Richer form (PRIMARY NEED / BEST PICK / OTHER GOOD PICKS
  / BAD PICK / CONFIDENCE / REASON, optional GEAR_KEYS; legacy YOUR PICK
  still parses; seed-20260812 parties byte-identical) + the full metric set
  (top-1 / top-3 / acceptable / ranks / need-agreement / bad-pick-rate /
  confidence-weighted — never one collapsed number).
- V4 evidence classes: `weapon_only` (legacy — still the exit-code gate
  pending the decision below) / `doctrine_inferred` (labelled inferred;
  doubly weak-form — the doctrine pools were mined from these same comps) /
  `actual_gear` (kits joined from builds_index — published comps carry gear
  on all 201 slots; 387/485 pieces resolve into the curated catalog,
  unresolved counted never guessed). Result: 78% legacy role-level collapses
  to 34% (doctrine) / 41% (actual) dressed; 92/92 slots change top-3; 12/32
  role hits lost, 11 of them tank drops.
- Root cause measured, not tuned (`audit_dressed_templates` +
  `audit_frontline_floor` + `audit_validation_asymmetry`, all report-only):
  worn-armor stats add +419–622% of the tankiness target in every template;
  ordinary doctrine kits alone clear the castle-outpost tankiness floor for a
  7-man with ZERO frontline weapons (0 → 15.88u vs floor 1.7, penalty 9.0 →
  0, weakness erased) — the 2026-08-12 pseudo-tankiness failure mode
  recreated through the gear stat channel. Classification: representation
  problem (category 1). Options A–D with source-aware floors (Option C)
  recommended.
- Synergy-source finding: gear-sourced caps forgo 0.14–0.45 score in
  minimal constructions and would move blap NEGATIVELY (−0.112) if gear
  counted (J rises under dressed vectors while target-capped sides do not).
  Recommendation: Model 2 — synergy stays weapon-only ("weapon-interaction
  synergy"); Model 1 would be a J-rule redesign, calibration-gated.
- Calibration layer scaffolded (`calibration/` with train / validation /
  holdout discipline; 4 train cases transcribed from round 1 — nothing
  invented; validation/holdout EMPTY until fresh rounds):
  `pipeline/calibrate_scoring.py` sweeps the Phase-8 box with a
  scoring-path self-check and per-point golden counts against a patched
  dataset copy (BION_DATASET, real dataset untouched). Result: SENSITIVITY
  MAP ONLY — train curves flat (n=4), golden pins robust across the whole
  box except rho=0 (the duplicate-penalty pin, by design), synergy bonuses
  cannot flip a two-candidate contest even at 3.0. Every coefficient stays
  at its shipped value, PROVISIONAL.
- Gear doctrine cards generated (`tests/gear_blindtest.py` → 16 cards,
  engine answers in a hidden key file, relative-ranking scorer) — awaiting
  a validation round.
- Adjacent gaps reported, not fixed: `forge()` hard-codes locked members
  naked (F23 pins it; the dressed-forge spec's locked-kit language is not
  yet true for forge locks); standalone `refine()` is gear-blind.

**Open questions raised.** (1) tankiness/frontline Options A–D — recommended
C, source-aware floors; (2) V4 gate re-basing to a dressed class
(recommended: after 1); (3) synergy source Model 1 vs 2 — recommended 2; (4)
the next V3-D round (fresh seed, richer form) to create uncontaminated
validation/holdout; (5) forge locked-gears / `refine()` intended behaviour.

### 2026-08-27 — Five dressed-validation decisions

All five decided the same day with behaviour defined; no coefficient,
template, sheet, style or meta number changed anywhere.

- Decision 1 — Option C SHIPPED (both ports, 60/60 parity at 1e-9).
  STRUCTURAL hard floors read the WEAPON+LOADOUT supply: `fitness` takes a
  naked-basis floor read when gears are present; every marginal path splits
  the same way (`_combo_score` on dressed parties; `_combo_score_dressed`
  always — a candidate's KIT can never buy floor relief either;
  `_marg_fit_from` / `_marg_fit_pre` grew the floor-basis parameters;
  `pick_report` floor_lift rows and `explain` follow; `_floor_gain` caches
  the weapon-side floor gains for the hot path). Gear still counts toward
  coverage / headroom / overstack; floor magnitudes untouched. Naked parties
  are bit-identical (V4 weapon_only 13/92 + 25/32 = 78% EXACT; V3-W
  byte-stable). Dashboard floor tags read the same basis (`supplyFloor` in
  `_app.js` / `_decision_layer.js`). Pins: `test_validation_modes.py`
  V5a–V5f (a no-frontline 7-man pays the full 9.0 penalty in doctrine kits
  AND in all-plate; one real tank repairs it; dressed pick score == dressed
  comp_score delta at 1e-9). Measured after: adversarial A/C floors bite
  dressed (old rule: cleared); V4 actual_gear role-level 41% → 47% (two
  Deadlyhooker tank drops recovered; 0 newly lost); the remaining dressed
  shortfall vs 78% is the soft-cap/target SATURATION question — category 5,
  parked for the calibration rounds. Found and fixed en route (structural,
  both ports): a pre-existing forge pruning blind spot — a pick could
  exhaust a role band that every remaining satisfier of an unmet predicate
  minimum needed (locked-Ironroot castle 7: the beam picked a second
  non-full healer, capping the band with primary_heal unmet, and DIED at
  6/7 members, feasible=False). `_forge_ctx` precomputes per-predicate
  (role, seat) capacity gates and `_forge_feasible` refuses band-stranding
  picks; F15 green again. Ordinal changes from Option C, fully enumerated
  (probe diff vs the committed artifact): 2 V4 role slots recovered; 2 of 12
  V3 seed-case DRESSED top-3s changed (cases 1/4 — small floor-armed parties
  where candidate kits had bought floor relief; case 1 now leads Hand of
  Justice over the kit-boosted Witchwork); the deprecated legacy-hybrid
  ordinals moved on the same two cases (intended); ZERO golden changes
  (57/57, no re-pins).
- Decision 2 — gate re-basing DEFERRED. The exit code stays on weapon_only
  role-level (78% PASS, unchanged). Post-fix dressed numbers:
  doctrine_inferred 11/32 = 34%, actual_gear 15/32 = 47%. Which dressed
  metric becomes the gate remains open, now with the floor bug out of the
  way.
- Decision 3 — Model 2 documented. `scoring.yaml`'s synergy block names
  the concept WEAPON-INTERACTION SYNERGY (engine rule 3, with the measured
  negative-participation citation); engine/README carries it; no J
  redesign; Phase-9 discrimination cases retained in
  `out/calibration_report.json`.
- Decision 4 — next round prepared. `generate` gained `--content` /
  `--style` and forms carry a machine-readable `FORM_CONTEXT` line that
  `score` reads (a round can no longer be scored under the wrong context
  silently). Fresh forms committed: `tests/tier2_form_r2_castle7.md` (seed
  20260827) and `tests/tier2_form_r2_blackzone20.md` (seed 20260828,
  blackzone_roam 20) — richer fields, engine output hidden; the 16 gear
  cards regenerated against the post-decision engine (answer key
  refreshed). Validation/holdout stay empty until real answers arrive;
  nothing synthetic is ever seeded.
- Decision 5 — both dressed-engine gaps FIXED (both ports,
  parity-carried). `forge(..., locked_gears=)`: a locked member with
  supplied gear is scored in EXACTLY that kit and never re-dressed; without
  gear it stays naked (nothing invented); legacy calls byte-identical (F23
  stands; F25 pins the contract + determinism + score == dressed
  comp_score). `refine(..., gears=)`: dressed local search optimising the
  same comp_score-with-gears everything else uses — incumbent kits
  preserved, replacements tried per doctrine kit variant, returns `{party,
  gears}`; `gears=None` keeps the legacy list-returning weapon-only path
  bit-identical (F26/F26a pin dict shape, monotone dressed score,
  convergence, fixed-slot gear preservation, determinism). Parity grew
  mirrored `refine_dressed` and forge `locked_gears` fixtures (60/60 +
  embed).

**Evidence.** Full battery: 57/57 golden (zero re-pins), 38/38 forge
(F25/F26 new), 18/18 validation-modes (V5 new), 18/18 roles, 32/32
interactions, 60/60 parity + embed, builds / provenance / patch / codec /
display / cohort / lint green, tier2 legacy 78% PASS. STILL OPEN: the final
gate choice (decision 2); the saturation / template-unit-scale calibration
(needs validation rounds); the round answers themselves (forms ready).

### 2026-08-28 — Reflect, self-costs, and the first super-additive duplicate

**Context.** Play observation: no team brings just one Demon Armor wearer —
they bring two or more to counteract the self-tankiness loss. Measurable and
mechanistic: the killboard quota miner had independently recorded reflect
shells at 3,3,4,3,3 across the five near-complete 20-man rosters — never
one — and the dumps explain why. Demon Armor's Protection of the Fiends is
the ONLY team reflect in the game: it returns 50% for the wearer AND up to 9
allies while granting them +0.43 resistances, and pays for that by cutting
the WEARER's own resistances by 0.37. Every other reflect in the catalogue
(Counter 150%, Parry 125%, Deflecting Spin 100%, Retaliate 85%, Inferno 38%,
Frost 15%) is self-only. Two wearers stand in each other's aura and the
penalty is repaid; one wearer just eats it.

**Changes.** (1) Reflect is structural — `reflectdamageactive`
(@amountpercent, @target) added to the effect catalogue's node extraction
and mapped self → [reflect], ally → [reflect, buff_allies]; 32 weapon lines
and 25 gear pieces carry it, and the lint grounds every claim. Curated on
its real sources: Demon Armor 6 (team-wide), the personal stances 2, Frost
Shield 1. (2) SELF-COSTS — a new `self_costs:` block on a sheet entry,
compiled into the dataset and charged in `build_extra` on the WEARER's own
vector (never the team pool), last so the stat channels cannot re-multiply
a cost, floored at zero. This is the field whose absence let a backwards
`tankiness` claim survive every review: the sheet could express the upside
and nothing else. (3) `self_cost_offset_min_copies` — the mirror of the
count-once rule and the ONLY super-additive duplicate in the model (a
duplicate is worth more only in special cases like Demon Armor):
verified-confidence only, scoring_note required, an int ≥ 2, and it may
CANCEL A COST but never add supply, so a duplicate still cannot out-earn two
independent first copies. The interactions layer gained gear equippability —
REFLECTAREA is its first GEAR record.

**Evidence.** A six-body party scores tankiness 12.00 naked, 11.72 with ONE
Demon Armor (the team is worse off) and 14.44 with two. NO TEMPLATE ROW for
reflect, deliberately: it measures 0.0 in the weapon + spell-pick unit for
all eight comp parties (the reflect Ws are never the default combo), and in
the person unit three comps field 9-12 while five field none — a genuine
disagreement, which the 2026-08-21 convention leaves untouched; it waits for
the unit re-fit, while the Demon Armor mechanism lands on tankiness, which
every template scores. Five new pins in test_interactions (37/37): the
record's discipline, that it is the only offset in the corpus, that one copy
costs the party, that two beat twice one, and that reflect supply itself
stays strictly additive. Full battery green (13 suites incl. 60/60 parity +
embed, golden, forge, tier2).

### 2026-08-28 — The style labels and three evidence-quality decisions

**Context.** Per-style targets need style-labelled comps, and 8 of 13
records carried no `style:` at all. The corpus was shown as rosters only (no
engine reading, validation-round protocol) and came back labelled in one
pass: hellgate5 clap; roads_oneshot clap_kite; sob_blaze_os clap_kite;
sortasaucy_7man_ftb clap (more clap than clap-kite, some kite elements — the
kite lean recorded in-file and NOT fitted); clonepeek_zvz20 brawl_clap.
Record-level styles went 5 → 10.

**Decisions** (three evidence-quality calls, each enforced): (1)
cb_kroozlt19_bomb_squad gets NO style — a bomb squad is a different kind of
party whose objective is maximum damage without much survivability
(Assassin Jacket's ability increases damage even though it is leather); it
carries `archetype: bomb_squad` (the archetype comp_identity already
models) plus `fit_exclude`, because folding a deliberately fragile comp into
clap would drag clap's durability targets down. (2)
cb_shadowlagrange_tracking5 gets no style and `fit_exclude`: a PvE comp for
tracking content answers a different question than any PvP template. (3)
deadlyhooker party_2 and party_3 are excluded (not built properly; would
give wrong data) while party_1 is labelled clap_kite leaning clap (the most
relevant).

**Changes.** The schema admits PER-PARTY style and exclusion: one record's
parties are not always one comp shape, and the record-level value is the
fallback. Both `style` and `fit_exclude` are validated per party (an
exclusion without a stated reason is rejected — indistinguishable from lost
data) and travel into builds_index so every consumer sees the decision;
tier2 honours them and prints the skip with its reason. Stale-list bug found
by the labels: `builds_lib` rejected `clap_kite` as an unknown style — its
whitelist predated the fifth playstyle and nothing had caught it because no
comp was labelled clap_kite until now.

**Evidence.** V4 drops from 25/32 = 78% to 17/23 = 74% role-level — still
PASS, and the drop is the point: it stopped grading itself against two
parties their own author disowns. Full battery green.

### 2026-08-28 — One comp, several contents

**Context.** Half the corpus states a content that names a FORMAT, not a
template (`zvz_20man`, `zvz_7man`, `zvz_20v20`, `large_scale_zvz`), so
those comps skipped every fit. The audit layer had been guessing a single
mapping (zvz_20man → blackzone_roam etc., flagged audit-only).

**Decision.** A 20-man ZvZ comp can do blackzone roaming, castle outposts
or castle defense; a 7-man can also do Avalonian roads; any comp doing
outposts, castles or blackzone roaming can do faction war as well.

**Changes.** `content_candidates: [...]` on the seven format-labelled
records (20-man → blackzone_roam / territory_defense / castle / faction_war;
7-man → castle_outpost / roads / faction_war), validated against the real
template list so a typo or renamed template surfaces instead of silently
dropping a comp from every fit, carried into builds_index. Applied ONLY
where the stated content names no template — transferability for the
explicitly-labelled comps is a separate call.

**Finding.** Measuring how much the six templates differ (targets
normalised per head) first suggested 19 of 30 capabilities were
"content-specific" — an artifact: only blackzone_roam and territory_defense
were ever comp-fitted (2026-08-21); castle, castle_outpost, faction_war and
roads still carry hand-set provisionals, and the spread was mostly
fitted-vs-guessed. Comparing ONLY the two fitted templates: 21 of 30 targets
agree within 25% (mean difference 20%), and six genuinely diverge — cleanse
(0.8 vs 7.2), engage (11.7 vs 4.2), mobility (19.8 vs 4.2), silence,
anti_dive, anti_zone. Every one is about MOVEMENT vs HOLDING GROUND: the
same 20-man ball roams, hits outposts and fights faction war; what changes
is whether it chases or holds. Consequences for the pending re-fit: (a)
multi-content evidence is not a contamination risk but the fix — it is how
the four never-fitted templates get real numbers; (b) the convergence worry
is mostly moot, honestly measured templates were already ~80% alike; (c)
content difference may be better modelled as a small set of movement /
holding modifiers over a shared base than as six independent 30-row tables.
Full battery green.

### 2026-08-28 — Wiki research pass and two closing decisions

**Context.** Four things came back from the wiki (fetched via Playwright —
it 403s scripts). (1) Disarray is listed as a core ZvZ mechanic beside
Focus Fire and AoE Escalation; already in mechanics.yaml with current data
incl. the Radiant Wilds battle-mount rule, and deliberately unwired for a
sound reason already recorded — it prices OUTNUMBERING, not composition (a
no-op in a mirror fight). No gap. (2) "Outpost" is two different
objectives: Faction Outposts (royal zones, towers with a capture ring, open
terrain) vs Castle Outposts (Outlands, guards → elite guards → Outpost Lord
→ claim circle, a smaller castle). That separates two axes that were being
conflated: faction war is OPEN TERRAIN but still requires HOLDING a ring, so
move-vs-hold and open-vs-enclosed are independent dimensions. (3) The armour
classes read as described in play — cloth "limited defense but excels at
offense", leather "balanced", plate "high defense but low on offense" — so
cloth-as-squishy-marker is the game's own framing; and "each armor piece has
one active and one passive ability", which confirms the passive-as-a-slot
design for the pending passive-scoring work. (4) Resistance is directional,
which the engine flattens into one tankiness number.

**Decision 1: do not split resistance.** The skew is real (leather exactly
equal at 98.2/98.2; cloth 59.4/76.0 and plate 154.0/120.6, both 22%) — and
it cancels. Classifying every weapon by SUBCATEGORY (the game's own rule:
melee/ranged physical, magic weapons magical) and weighting by the damage
each actually supplies, real comps field a 50/50 mix (268 vs 266 damage
points), at which the two skews cancel EXACTLY — all three classes price
identically whether averaged or weighted (0.0% error). Self-correction the
same day: the first pass classified by E-description text, got 62/38 and
~3% error, and was committed before the flaw was found — that sample was
biased twice over (the regex missed shapeshifters, whose damage lives in the
transformed form, and utility-E weapons, whose damage sits on their Q/W; and
it counted BODIES rather than damage, so healers holding holy staffs voted
"magical" while supplying none). The conclusion held but the evidence was
wrong, so both records were corrected. Splitting would double every
durability row for no correction at all.

**Decision 2: the app models no enemy** (standing rule 11). The party's
playstyle dictates how it fights regardless of who or where the opponent
is; no notion of the expected enemy is needed. Both decisions are recorded
at the summation site in mechanics.yaml so neither is re-litigated. This
also closes the open peel question's third branch (peel tracking enemy
dive threat): with no enemy model, peel must be explained by comp or
content alone.

### 2026-08-28 — Four decisions

(1) clap/clap_kite ranged core = 7 at 20 (7 chosen over the recommended 6)
— wired into styles.yaml constraint_overrides (merged into the existing
first-match rows; 15-19 carries the scaled interpolation 5), F21 extended;
the forge's ranged styles now guarantee seven members whose SELECTED spells
deliver ranged AoE (combo-aware, the F12 predicate — flex weapons like
Realmbreaker count by their ranged E, so the seat labels still show melee
bodies; capping melee SEATS under ranged styles would be a new
style-dimension profile, noted as a follow-up). (2) Icicle stays
zone_support — the earlier mention of Bedrock and Icicle as d-tanks was
loose usage, no membership change; recorded so the question never reopens
silently. (3) Healers at 5-7 stay 1-2 — the data-mode cap declined; the
forge may field a second healer at 7. (4) Effect quotas GRADUATE to advice:
the dashboard's killboard-evidence strip gains "Observed effect quotas" —
per typed gear effect, the roster's LOADOUT chests counted against the
median carriers near-complete observed rosters field (scaled to PLAN, armed
15+; members without gear set are counted as unknown and never produce a
shortfall claim; language + placement follow the killboard evidence rules —
advice only, nothing scores). build.py computes the medians from
roles_report effect_quotas at build time; display-math case 14 pins the
counting, scaling, unknown-gear honesty and arming (14/14). Full battery
green after all four.

### 2026-08-28 — Gear resolver: item ids and caller shorthand

**Context.** Measured rather than guessed: the evidence layer was silently
discarding most of the gear the comps record. `match_gear` matched only
DISPLAY NAMES, but 11 of 13 published comps write gear as game item IDs
(`ARMOR_CLOTH_AVALON`, `HEAD_LEATHER_SET3`, `CAPEITEM_SMUGGLER`). Those
cells resolved to nothing at all: 729 of 1124 gear cells were ID-style and
zero of them resolved. Only Deadlyhooker and blap (display-name sources)
carried gear into the index.

**Changes** in `pipeline/builds_lib.py`, all identity or spelling facts:

- Item-ID matching (`_match_gear_key` / `key_form`) — an ID IS the catalogue
  key, so it is an identity match. Tier prefix and enchant suffix are
  normalised off both sides (sources write `MEAL_STEW`, the catalogue says
  `T8_MEAL_STEW`). Slot-checked; returns None on a key collision rather than
  picking one.
- Caller shorthand (`GEAR_ALIASES`, slot-scoped), from the callers' own
  usage: "GG" on the boot slot is Graveguard Boots; "blink" is Stalker Shoes
  (the double blink); "cleanse" is a leather helm with the second ability
  (→ Hellion Hood). Spelling facts: `RoP` → Robe of Purity, `Smugglar` →
  Smuggler Cape, `helm` → `helmet` (`WORD_FIXES`).
- Notation — a leading tier marker is stripped (`TIER_NUM_RX`: "7.1
  omelette", "8.1 beef stew", "T8 Beef Stew"); no catalogue name begins with
  a digit. `split_alternatives` learned `" or "` alongside `/`, so "Caitiff
  or Aegis" becomes a primary plus a recorded alternative.

One entry is INFERRED, not decided, and flagged for confirmation: a bare
`omelette` is ambiguous in the catalogue (Pork / Avalonian Pork, both T7)
and is listed as the plain line because callers name the Avalonian one
explicitly ("ava pork omelette"). It sits in the alias table where it can be
overruled.

**Evidence.** All 13 comps now carry gear (was 3). Corpus-wide, 0 of 1117
recorded pieces are unresolved and 1086 = 97.2% reach a CURATED record (the
number that matters — only curated pieces carry capabilities; the prior
figure was 387/485). Resolution is not supply: 31 pieces resolve to real
game items with no capability sheet and contribute ZERO supply — 20×
`T5_POTION_REVIVE`, 6× `T8_MEAL_STEW_FISH`, 4× `T7_MEAL_OMELETTE_FISH`, 1×
`HEAD_GATHERER_HIDE`. Two earlier claims retracted: the 11 ID-style comps had
resolved NO gear (not "already 100% resolvable"); and the suspected
`split_alternatives` escape-sequence bug (`"n\a"` for `"n/a"`) was a ripgrep
rendering artifact — the file was correct. Full battery green, no re-pins;
tier2 78% weapon_only unchanged (it reads gear through its own ID-aware
reader, which is why it disagreed with the builds index and exposed this).
Unblocks the unit re-fit on data completeness: the person-unit measurement
now has 14 sources and ~1086 curated pieces behind it instead of 3 and 629.

### 2026-08-28 — Optional capabilities

**Decisions.** anti_zone is supplied by one weapon (the crystal healing
staff) and brought by some ZvZ groups, usually at 30+ people: keep the
targets but make the row optional. Execute likewise optional. Peel is about
how the party fights (deferred to the peel record below).

**Evidence.** Verified before implementing: `anti_zone` is supplied by
exactly ONE item in the catalogue — Exalted Staff, `2H_HOLYSTAFF_CRYSTAL`,
citing HOLY_DISPEL; `execute` by five single-target melee weapons
(Bloodletter, Prowling Staff, Kingmaker, Infernal Scythe, Broadsword).

**Changes.** `optional: true` on a template requirement row (both ports, all
six templates that carry either row). Bringing the capability earns its
coverage exactly as before; not bringing it is not a hole. A
DENOMINATOR-ONLY rule, provably: every fitness term for a capability at zero
supply is already zero — coverage is `min(1, 0/target)^gamma`,
`_headroom_bonus` returns 0 unless `have > target`, `_overstack` needs `have
> soft_cap`. An optional capability can only leave `max_fitness()`, never
`fitness()`. The one term charged at zero supply is `_floor_penalty`, so
optional + hard floor is contradictory by construction; both ports raise on
that combination (no floor exists on either capability — floors are only on
heal_sustain and tankiness). `max_fitness(party, combos, gears)` gained the
party-aware form (no party → the every-capability supremum, legacy callers
bit-identical). The dashboard passes the same loadout and gear the numerator
uses, and the radar drops an absent optional capability from its axis.
Consequence: no score, ranking, pick value or forge decision moves; 57/57
golden, 38/38 forge, 60/60 parity + embed (+ a `max_fitness_party` case),
tier2 78% weapon_only PASS. Displayed fitness moves +0.2 to +3.3 across the
ten measured comps; roads moves most (it carried `execute` at weight 4, the
heaviest row nobody fields).

**A finding corrected.** An earlier report had peel as content-shaped ("the
two roads comps field 5× what everyone else does") — a ratio misread as a
measurement. Per person, peel is flat at 3.4–5.1 across every style, content
and size; the two roads comps sit at 4.07 and 4.36, mid-pack. What differs is
the TARGET: roads asks 0.37 peel per person where blackzone_roam asks 2.01,
a 5.4× spread — the templates disagree about peel far more than real comps
do; same shape in silence (0.13–1.01 per person) and zone_control
(0.19–0.45). Corrected in
`notes/findings/2026-08-28-fielded-supply-per-person.md`. Standing hazard
for the re-fit: every supply/target RATIO carries two meanings ("comps bring
a lot" or "this template asks little") and only raw per-person supply
separates them; the re-fit must be done on raw per-person numbers, never
ratios. The peel decision itself stands on game knowledge, not the corpus:
the data shows no content effect and no style effect, so there is no fitted
number to hang a per-style peel modifier on; NO number invented in the
interim.

### 2026-08-28 — Peel is CC, not protection

**Context.** Knight Boots and Cleric Cowl carried peel scores; the challenge
was that peel should directly affect enemies. Checked before answering, and
the challenge was half right: Cleric Cowl's Force Field sends out a
shockwave knocking back all enemies within a 6 radius by 9 (ignoring CCR) —
a point-blank enemy knockback, so its peel is correct; Knight Boots' Shield
Charge charges toward the target and applies a shield to the caster (and an
ally target) — it never touches an enemy. That one was a genuine miss, and
it opened a whole class. The design doc already agreed
(albion-comp-engine-design.md §2.2): `clump_create` and `peel` sit in
Control because they are the two directional uses of CC (offensive stacking
vs defensive protection). Peel was DEFINED as crowd control; the effect
layer had drifted.

**Decision** (from three offered options): peel = enemy CC, plus cancelling
enemy CC on a teammate. Plain damage protection is NOT peel — it is
buff_allies / tankiness. Immunities, CC-resistance and diminishing-returns
grants KEEP peel (they defeat the enemy's CC); resistance buffs, absorb
shields, damage redirection and invincibility LOSE it.

**Changes.** `effect_map.yaml`: `physicalarmor+`, `magicresistance+`,
`bonusdefensevsplayers+` and `invincibility` on an ALLY map to
`[buff_allies, tankiness]` instead of peel — which also removed a live
contradiction, since `hitpointsmaxbonus+` on an ally was ALREADY
`[buff_allies, tankiness]`. `effect_overrides.yaml`: four hand-written peel
overrides removed, each encoding the overruled assumption in its own
justification (FORCESHIELD, EMERGENCY_SHIELD, CHARGE_SHIELD, SOUL_LINK);
every enemy-facing override KEPT (PBAOE_KNOCKBACK, WINDWALL, CASTBUBBLE,
ENFEEBLEAURA). The lint then found every affected claim by itself; five
went ungrounded and were removed, none replaced by an invented number:
`ARMOR_PLATE_KEEPER` Judicator Armor peel 4 (the highest peel score on any
gear, resting on Force Shield's +25% ally resistances); `HEAD_PLATE_SET3`
Guardian Helmet peel 2 (Emergency Shield); `SHOES_PLATE_SET2` Knight Boots
peel 2 (Shield Charge); `2H_NATURESTAFF` Nature Staff peel 2 (ally
resistances); `MAIN_NATURESTAFF_AVALON` Ironroot Staff peel 4 (Soul Link).
Consequence flagged: Ironroot loses peel entirely (its sheet comment had
called it "the soul-link peeler"); Soul Link redirects half the focus
damage off an ally without doing anything to the enemy, so if Ironroot
should still read as a protector the honest fix is a buff_allies/tankiness
magnitude, NOT a restored peel row.

**Evidence.** Peel per person across the ten dressed comps falls from
3.43–5.11 to 2.14–3.86 — roughly a third of what the model counted as peel
was damage protection. The blackzone comps sit much closer to their own
2.01-per-person target (push_monkey 2.14 against 2.01). Full battery green,
zero re-pins (the golden cases depend on none of the five claims). Open:
`SHAPE_W_TETHERBEAM` keeps its peel override ("pulling an endangered ally out
of danger") — ally-repositioning is neither enemy CC nor CC-cancelling but
not damage protection either; flagged rather than silently resolved.

**Second pass, same day — the loose ends closed.** (1) Tether Shift — peel
removed, consistent with the decision: the whole spell applies a 300-damage
absorb shield to an ally and pulls a party ally to the caster; attached to
an enemy it dashes the CASTER toward the enemy. Nothing touches an enemy or
cancels enemy CC, so it falls with the absorb-shield class; re-homed as
`buff_allies`. (2) The shapeshifter pool's peel was REAL but mis-cited —
fixed, not deleted: removing the Tether Shift override ungrounded `peel 2`
across the whole shapeshifter line (9 sheets, one shared pool row), and the
row's own comment named the true source, Polymorph — "transform[s] the first
unmounted, not transformed player hit into a helpless animal for 2.5" — hard
CC on an enemy. The parser had captured only the mob-case max-health debuff
(the transformation is invisible to the structured vocabulary, which knows
the concept only in negative form: `immunity:transformationcc`,
`dr:DRTransformationCC`). `SHAPE_W_POLYMORPH.peel` added to
`effect_overrides.yaml` as a documented parser misfire and the pool row
re-cited to it at the SAME score — a true capability nearly deleted because
its citation was wrong; the fix was to correct the citation. (3) Ironroot
Staff's identity resolved on the honest axis: peel 4 → `buff_allies 4`,
header comment "the soul-link protector". The magnitude is ANCHORED, not
invented: Brier of Life (2H_NATURESTAFF, same file) scores `buff_allies 4`
for +13% resistances on one ally; Soul Link halves incoming damage on one
ally — strictly stronger protection on the same single target — so 4 is a
floor under the sheet's own scale. Swept for the same class of hole: no peel
row anywhere cites the `GEAR_STATS` / `WEAPON_STATS` sentinel (which the
lint skips by design), so all 83 surviving peel rows are map-verified. Peel
per person unchanged from the first pass (2.14–3.86) — the re-citation
restored exactly what the tether removal took. Full battery green, ZERO
re-pins, tier2 78% weapon_only PASS. Deliberately still uncurated: the 31
recorded pieces that resolve to real items with no capability sheet
(recorded as a curation gap, per the say-we-do-not-know rule; see the next
record for the potion among them).

### 2026-08-28 — Tier-agnostic gear lookup

**Context (a correction first).** The 31 uncurated pieces had been reported
as items with no home in the capability vocabulary, `T5_POTION_REVIVE` (20
uses, every blap member) among them as a resurrection potion. That was wrong
on both counts — the item ID was read instead of the item. The game data
names `T5_POTION_REVIVE` the Gigantify Potion (Albion reuses the legacy
`REVIVE` id for it) and it is FULLY CURATED at `tankiness 2`, under
`T7_POTION_REVIVE` (Major Gigantify). The comps run the plain tier, the
sheets curated the Major tier, and an exact-key lookup scored 20 real
potions as zero. A bug, not a vocabulary gap. Root cause, the day's recurring
shape: consumables are curated at ONE representative tier (one row per
potion type: `T7_POTION_REVIVE`, `T8_POTION_BERSERK`, `T6_POTION_ENERGY`…)
while comps record whatever tier they ran; `builds_lib.match_gear`
deliberately prefers the PLAIN tier when a name matches several — correct
for recording what was worn — but the ENGINE then looked the key up exactly
and found nothing.

**Decision** (from three options): ignore tier everywhere. Tier is not part
of an item's identity in this model — a 1..7 sheet score is far coarser than
the tier ladder, and the sheets already curate one tier as the
representative of a whole line.

**Changes.** `gear_key()` in both ports: exact key wins; otherwise fall back
to the tier-and-enchant-stripped form (`_key_form` / `keyForm`, mirroring
`builds_lib.key_form` added earlier the same day). The alias index is built
only for UNAMBIGUOUS forms — if two curated items share one, the form is
dropped and the lookup fails rather than guessing. Applied at every gear
entry point: `gear_extras`, `build_extra`'s stat channels and doctrine
passives, the self-cost loop.

**Evidence.** `T3/T5/T7_POTION_REVIVE` all resolve to the curated Gigantify
(`tankiness 1.0` supply each); `T5_POTION_STONESKIN` → Resistance Potion;
`T5_POTION_CLEANSE2` → Cleansing Potion; exact keys such as
`ARMOR_CLOTH_AVALON` unchanged. blap's 20 members carry the potion the model
already knew about — party tankiness 75.56. Full battery green with ZERO
re-pins: no golden fixture used a non-representative tier, so the change is
additive to real comps only. The four items named: `T5_POTION_REVIVE` ×20 =
Gigantify Potion (blap) — FIXED; `T8_MEAL_STEW_FISH` ×6 = Deadwater Eel
Stew (metabattle); `T7_MEAL_OMELETTE_FISH` ×4 = Dusthole Crab Omelette
(blap); `HEAD_GATHERER_HIDE` ×1 = Adept's Skinner Cap (metabattle;
gathering gear in a combat build — correctly out of scope). STILL OPEN,
deliberately not guessed: the two fish meals have no sheet at ANY tier, so
tier-aliasing cannot reach them; the pipeline does not carry meal nutrition
(meals appear in `gear_lines.json` with name and slot only; the four curated
foods were scored by judgment citing the `GEAR_STATS` sentinel, which
`evidence_lint` skips). Closing this needs the real food bonuses (wiki via
the Playwright MCP, or a dumps re-parse that captures nutrition) — 10
pieces, ~1 supply unit each.

### 2026-08-28 — Per-style target modifiers: the mechanism

**Context.** The standing case: a clap comp requires more peel and
disengage than a brawl comp. styles.yaml could not express that: its
`multipliers` scale a capability's WEIGHT (what a style VALUES) while every
style in a content shared identical TARGETS, by explicit design. The model
could say a clap comp cares more about disengage, never that it needs more
of it before it counts as covered.

**Changes.** `target_mults` on a style (both ports,
`test_validation_modes.py` V6a–V6f). Semantics, pinned: target and soft cap
scale TOGETHER, so the headroom band keeps its shape (kite disengage 6.3 →
12.6 and 9.2 → 18.4 at a 2.0 multiplier); unlisted capabilities untouched;
HARD FLOORS NEVER SCALE — a style may change what it emphasises and how much
it needs, never what keeps the party alive (V6e pins tankiness/heal_sustain
floors identical under a styled engine); balanced is the identity by
definition and must stay empty. EVERY STYLE SHIPS EMPTY at this point: full
battery green with zero re-pins, and V6a is a standing guard that fails if a
value ever appears without a recorded decision. Both ports agree on an
injected multiplier.

**Why no numbers.** The corpus cannot fit them. Of the eight published
blackzone_roam comps, brawl has 3 and kite 2, while clap, brawl_clap and
clap_kite have exactly ONE each. Per-person means by style (blackzone_roam
only; both roads comps are clap_kite):

```text
                     brawl  brawl_clap   clap  clap_kite   kite
(n comps)                3           1      1          1      2
disengage             0.77        1.25   1.50       0.65   1.64
peel                  2.92        3.08   2.29       3.42   3.60
tankiness             3.06        2.12   3.05       1.93   2.17
engage                1.54        0.85   0.93       1.55   0.75
mobility              2.34        1.50   1.00       1.77   1.61
stun                  0.89        0.48   1.62       1.36   1.77
```

DISENGAGE agrees with the case: brawl is lowest at 0.77 and clap (1.50) and
kite (1.64) roughly double it, and kite is the one non-brawl style with
replication (n=2), so it is the strongest candidate for the first value.
PEEL does not: clap fields the LEAST peel of any style (2.29 vs brawl's
2.92) — on n=1, not a refutation, but exactly why the number has to be
decided on evidence rather than fitted. Nothing invented in the interim.

### 2026-08-28 — Per-style targets: derived from prior decisions, two thirds rejected

**Context.** Three routes tried. (1) External sources — nothing usable: web
search returns weapon tier lists, not composition requirements; the one
"ZvZ Clap and kite basic composition" page (albiononlinegrind) is a user's
18-build scrapbook mixing Crystal Arena and fame-farm builds; MetaBattle has
no composition pages at all (`list=search` for "ZvZ composition" → totalhits
0). (2) Killboard rosters — blocked by CIRCULARITY: labelling the 139
near-complete observed rosters with the engine's own `comp_identity` and
then measuring supply per style would reproduce the definition, since
`comp_identity` computes `evade += mobility + disengage` to decide the kite
half (IDENTITY_HYBRID_EVADE); `roster_mixes.json` is also stored as seat
counts, not weapons. Abandoned. (3) PRIOR DECISIONS — where the numbers
were: the model already contained style-varying requirements in seat units
from the 2026-08-23/26 forge rounds (`constraint_overrides`: healer 3-4
brawl / 2-3 clap / 2 kite at 20; frontline bands; `ranged_aoe_core` min 7
clap / 5 kite at 20) against composition.yaml's style-blind base bands —
and the targets ignored all of it (`heal_sustain` asked 7.50 of every style
alike). The forge was told a kite 20-man runs 2 healers while the scorer
demanded brawl-level healing.

**Evidence.** Multipliers derived as (decided seat count) / (base band
count) at each decided size, then TESTED against the published comps: does
coverage (supply/target) cluster tighter across styles, or scatter?

```text
capability      spread before -> after      verdict
burst_aoe            2.38 -> 1.58           KEPT
heal_sustain         1.93 -> 2.56           REJECTED
heal_burst           2.04 -> 3.34           REJECTED
tankiness            4.02 -> 4.93           REJECTED
```

Two thirds of the derivation failed its own test, and that is the finding:
the seat-count mapping only holds where the seat count IS the capability.
`ranged_aoe_core` counts members satisfying a burst-AoE predicate, so it
maps almost one-to-one onto burst_aoe supply. Healer count does NOT predict
healing supply — real kite comps already over-cover healing relative to
brawl (1.74 and 1.13 vs blap's 1.91) because gear, off-heals and support
weapons carry much of it. Frontline count does not predict tankiness at all:
the known unit defect (worn armor gives every member tankiness regardless of
seat) — nothing about tankiness can be fitted before the unit re-fit.

**Changes.** The one derivation that survived, cited to the decision it
comes from: `clap` and `clap_kite` `burst_aoe: 1.71` (ranged_aoe_core
7-at-20 and 5-at-15-19 vs base 4 and 3 — ratios 1.75, 1.67), `kite`
`burst_aoe: 1.29` (5 and 4 vs base 4 and 3 — 1.25, 1.33); brawl and
balanced stay the reference at 1.0. Rationale: a clap comp is REQUIRED to
field ~1.7× the base burst-AoE core, so scoring it against the base target
handed it free over-coverage for doing the one thing its style demands.
Independent corroboration: tier2 leave-one-out against published comps,
which had no part in the derivation — weapon_only role-level 74% → 78%
(17/23 → 18/23), weapon-level 15% → 18%; actual_gear role-level 39% → 43%
(9/23 → 10/23). Full battery green, zero re-pins. V6a now PINS the shipped
set. Still undecided and deliberately empty: brawl and balanced (the
reference), brawl_clap (no constraint override exists for it), every
capability other than burst_aoe — including peel and disengage (no decided
seat count maps to them; the corpus has n=1 for clap; the killboard route is
circular for exactly those two).

### 2026-08-28 — Per-style targets, round 2: a second independent sample

**Context.** Eight of the thirteen comps cite albioncompo.com, which has
130 public comps behind a clean JSON API (`/api/compositions/<CODE>`, found
by watching the page's own network calls) carrying full item IDs per slot —
weapon, helm, armor, boot, cape, offhand, potion, food. Triage: 96 are ZvZ,
27 usable (ZvZ, n ≥ 10, every weapon in the catalogue, every member geared).
A NON-CIRCULAR STYLE LABEL: many authors NAME the style themselves ("Brawl
Hit 20", "BRAWL BY LATTEX", "Hard ZvZ Brawl 20ppl", "ss kite", "kite deff",
"HDCP BOMB", "RANGED BOMB", "SIEGEBOW BOMB", "BUS BOMB", "Brawl Clap",
"BUILDS FOR BRAWL BOMB"); the label comes from the author, not the engine
("bomb" reads as clap — styles.yaml's clap blurb is "stack them and delete
them — one engage, one bomb"). 13 comps carry a declared style: clap 5,
brawl 3, kite 2, brawl_clap 2, clap_kite 1. Clap went from n=1 to n=5.

**Evidence.** Independent confirmation of the shipped burst_aoe values
(derived purely from the seat counts; the author-labelled sample shares no
input with that derivation):

```text
style        shipped   observed/brawl    gap
clap            1.71             1.73   +0.02
clap_kite       1.71             1.62   -0.09
kite            1.29             1.51   +0.22
brawl_clap         -             1.22   (undecided, stays 1.0)
```

Two derivations with nothing in common agreeing to 0.02 on clap is the
strongest validation any number in this model has. The original case — clap
requires more peel and disengage than brawl — tested across BOTH samples as
a ratio to each sample's own brawl:

```text
peel        published  declared   agree?
brawl_clap       1.05      1.01   yes
clap             0.78      1.07   NO - flips
clap_kite        1.17      1.29   yes
kite             1.23      1.27   yes

disengage   published  declared   agree?
brawl_clap       1.62      0.94   NO - flips
clap             1.95      0.64   NO - flips
clap_kite        0.84      1.10   NO - flips
kite             2.13      1.17   yes
```

**Decision.** CLAP is the half that fails: both of its claims flip sign
between samples — two independent samples disagreeing is noise, and no clap
row is written. KITE is the half that holds, replicated on both: `peel
1.25` (1.23× brawl published, 1.27× declared; `clap_kite` gets the same
1.25 — 1.17×, 1.29×); `disengage 1.20` — direction replicated but magnitude
unstable (2.13×, 1.17×), so the CONSERVATIVE end; kite only (clap_kite flips
and gets no row).

**Changes.** styles.yaml `target_mults`. tier2 improved again: weapon_only
role-level 74% (before any target_mults) → 78% (burst_aoe) → 83%
(peel/disengage), weapon-level 15% → 18% → 24%. Full battery green, zero
re-pins. Test note: V6b/V6c inject a multiplier and compare against a
baseline; once kite gained shipped values its baseline stopped being 1.0
and those cases FAILED, correctly — they now inject into `brawl`, a true
identity reference. Still undecided: `brawl_clap` (no constraint override,
only n=2 declared) and every clap row beyond burst_aoe. The 27 usable comps
were not yet ingested into `data/published_comps` — the durable next step.

### 2026-08-29 — Corpus ingestion: 13 → 36 published comps

**Changes.** 23 new records in `data/published_comps/`, same format as the
existing albioncompo entries: slot rows VERBATIM (game item ids as the site
serves them), spells `null` because the source has no spell fields, full
provenance (share url, author family from the account id, retrieval date,
view count, `is_public` licence note). Four excluded on quality: three where
a weapon is not in the catalogue, three where not every member is fully
geared, and one the AUTHOR marked unfinished ("Kite clap 7 man+ (Not
finished)") — a comp its own author calls unfinished is not evidence of a
real one. Style labels are AUTHOR-DECLARED, taken from each comp's own name,
never from the engine's classifier; comps whose names declare nothing carry
`style: null`.

**Evidence.** Corpus 36 comps (was 13): brawl 6 (was 3), clap 6 (was 1),
clap_kite 3, brawl_clap 3 (was 1), kite 3 (was 2), unlabelled 15. Every
thin style now has n ≥ 3. Build records 237 → 625; canonical defaults 55 →
112; no new quarantines. THREE GATES FIRED, all correctly:

1. `build_dataset` refused the build (exit 2, provenance FAIL): a
   MASTERSHEET/roles kit override adds `OFF_TORCH_CRYSTAL` to
   `brawl_healer/offhand`; with the new comps that item is in the seat's
   OBSERVED doctrine tier on its own, so the hand entry became redundant and
   the gate refused to ship a decision that no longer does anything. The
   2026-08-26 decision was VINDICATED, not reversed — the evidence caught up
   with it. Override removed, reasoning kept in a comment, tier unchanged.
2. `test_roles` R18 pinned Polehammer's armor doctrine at `[5, 5]`; it is
   `[9, 12]` now. The MECHANISM it guards is intact — the weapon's own
   observed kit still outranks the seat aggregate and still resolves to
   Knight by a clear majority. Count re-pinned with a note that
   `doctrine_n` tracks corpus size.
3. `test_builds` H16 asserted excluded weapons have no build records AT
   ALL. A new comp ("AvA Raid", 10 players) genuinely fields
   `MAIN_FROSTSTAFF_AVALON`, on the ≥ 10 exclusion list — a real record that
   contradicts the exclusion's own stated reason ("no caller sheet,
   published build or observation fields them at party size >= 10"). The
   assertion was stricter than the design it guards: composition.yaml's
   documented exit lifts an excluded weapon when it gains an APPROVED
   CANONICAL build, via the evidence gate and a decision. Split into two
   checks — approved/canonical records still fail hard (silent
   re-admission), while known candidate evidence is recorded explicitly so a
   NEW one is still visible. The exclusion STANDS (the record is candidate;
   `exclusion_gate` correctly empty). OPEN: the premise for excluding
   Chillhowl at 10+ is weaker than when it was written.

tier2 improved for the third time in the day, with no part in any of this:
weapon_only role-level 74% → 78% → 83% → 87%; weapon-level 15% → 24%;
actual_gear role-level 39% → 57% (the biggest single jump, from the gear
evidence). Full battery green. Not yet done: the per-style multipliers were
fitted when clap had n=1 and should be re-derived at n=6.

### 2026-08-29 — Re-derivation on the 36-comp corpus: nothing changed, deliberately

**Evidence.** The per-style multipliers re-derived from the CORPUS itself
(clap n=4-6 labelled at n ≥ 10):

```text
style       cap          shipped   re-derived    gap    n
clap        burst_aoe       1.71         1.93   +0.22   4
kite        burst_aoe       1.29         1.75   +0.46   3
kite        peel            1.25         1.22   -0.03   3
kite        disengage       1.20         1.36   +0.16   3
clap_kite   burst_aoe       1.71         1.92   +0.21   1
clap_kite   peel            1.25         1.23   -0.02   1
```

`peel` re-measures to within 0.03 of what shipped on an independent basis;
the rest sit below the newly measured effect, so the model UNDER-states every
style difference it models, never over-states one. The larger sample also
revived a rejected derivation: at n=3-5 the healer-count → healing-target
mapping matches observation closely (clap heal_sustain observed 0.73 against
the 0.82 the healer counts predicted, kite 0.64 against 0.61) — the earlier
rejection was a small-sample artifact. New signals: `slow` runs clap 1.73 /
clap_kite 2.33 / kite 1.51 against brawl. Four candidate sets tested on
tier2 leave-one-out (the arbiter — it holds a published comp out and asks
the engine to rebuild it, so it is not the thing being fitted):

```text
A  shipped (baseline)                       weapon_only 15/62 role 20/23 | actual_gear 13/23
B  kite burst_aoe 1.29 -> 1.50              weapon_only 15/62 role 20/23 | actual_gear 13/23
C  + healing (the revived derivation)       weapon_only 15/62 role 20/23 | actual_gear 13/23
D  + slow (clap 1.73 / kite 1.51)           weapon_only 15/62 role 20/23 | actual_gear 13/23
```

**Decision.** Identical to the case; golden clean in all four. The gate
cannot tell these apart, so there is no evidential basis to prefer any of
them — adding multipliers the gate cannot validate is fitting numbers to
taste. Nothing changed. The flatness is itself the finding: tier2 has
SATURATED as a discriminator for per-style targets at this corpus size (it
moved four times in the day, 74 → 78 → 83 → 87% role-level, and now does not
respond to further per-style tuning). Sharpening these numbers needs a
different instrument — more held-out labelled comps, or validation rounds —
not more of this one.

### 2026-08-29 — The unit re-fit

**Context.** The long-standing defect (HANDOFF.md "KNOWN UNIT DEFECT"): every
target and soft cap was fitted counting WEAPONS while the engine measures
whole dressed PEOPLE, so the two sides of every comparison were in different
units. tankiness read 4.2–6.6× target in every published comp.

**Decision.** 152 rows across all six templates moved together (the "one
unit, everywhere" rule — a partial move would leave the templates in mixed
currencies). `weight`, `scales` and `optional` untouched. Hard floors
untouched: Option C makes floors read the WEAPON+LOADOUT supply only, so
they are already in weapon units by design and must stay there.

```text
                       before    after
tankiness coverage    4.2-6.6  1.19-2.12     <- the defect, fixed
median coverage           n/a       1.82     (still conservative)
tier2 weapon_only         87%        70%     (naked; the retired unit)
tier2 doctrine_inferred   26%        87%
tier2 actual_gear         57%        87%     (real recorded kits)
```

**Evidence — three failed attempts, and why each failed.** (1) Re-derive
every row from scratch (0.9× least / 1.15× most over all 28 dressed comps):
REJECTED — the convention says "the least any GOOD comp fields", and across
a mixed-quality corpus the WEAKEST comp sets the target (heal_burst
collapsed to 0.11× because one comp fields almost none). (2) Scale by the
median per-comp dressed/naked ratio: REJECTED — the ratio varies 1×–27×
across comps, so the median overshoots for high-variance capabilities; it
pushed blap (a melee ball that deliberately carries little escape) from 1.11
disengage coverage to 0.37. (3) Scale by min(dressed)/min(naked) and
max/max: REJECTED as the whole answer — min-based ratios are outlier-driven
where the minimum naked supply is near zero (engage ×3.50, mobility ×4.55
off single comps), and 3 of 4 reference comps ended up BELOW the tankiness
target.

**What shipped.** Fit on the hand-labelled comps only (the 11 labelled and
decided on, minus the two Deadlyhooker parties excluded from fitting) — 9
fully-dressed comp-parties — measured DRESSED, style-normalised, with the
standing formula. Two principles fixed it: a UNIT CONVERSION CAN ONLY ADD —
gear never removes supply, so a factor below 1.0 is not a unit correction but
a separate claim that the old target was miscalibrated; twelve came out
below 1 and were CLAMPED to 1.0 (without the clamp burst_st's target fell
47% and forge F3 broke: the cross-member `resist_shred × burst_st` synergy
saturated on a single member, so a real interaction silently priced at
zero). Only 13 of 29 capabilities move at all, exactly the gear-fed ones:
buff_allies ×4.63, tankiness ×3.88, cleanse ×3.00, sustained_dps ×2.59,
anti_zone ×2.25, anti_dive ×2.14, stun ×2.11, damage_debuff ×1.50,
resist_shred ×1.35, knockback_displace ×1.23, root ×1.11, clump_create
×1.03, heal_sustain ×1.02. The other 16 keep their targets exactly — peel,
disengage, mobility, engage, catch, burst_aoe and the rest were already
right in person terms; only the capabilities worn armor supplies were
wrong — a more precise statement of the defect than HANDOFF.md's.

**Changes.** Two golden re-pins, both intended: T26 fight chain graded blap
NAKED and expected all-strong — against person-unit targets a naked party
correctly reads weak, the same unit error the re-fit removes; re-pinned to
grade blap DRESSED in its own doctrine kits (what the page does): all five
stages strong. T30d heal band moved overstacked → headroom as the soft cap
rose; the load-bearing judgments (neither of two healers redundant, a third
still redundant, tank clean, fitness untouched) unchanged. Full battery
green. OPEN: `tier2`'s exit gate was still legacy `weapon_only` role-level
(naked incumbents), which the re-fit necessarily degrades 87% → 70% —
measuring in the unit the model just left; it PASSES exactly on its
threshold while both dressed classes jumped to 87%. Median coverage 1.82:
targets remain CONSERVATIVE (good comps over-cover) — a calibration
question, no longer a unit question.

### 2026-08-29 — Gate re-based to actual_gear, and it enforces

**Decision.** The tier2 exit gate reads `actual_gear` role-level (70%
threshold unchanged): that class scores incumbents in the gear their
published source records — what the page does — and is the only class
whose incumbents are not mined from the same doctrine the engine uses
(`doctrine_inferred` is doubly weak-form by construction).

**Changes.** Second, and more important: THE GATE NOW ENFORCES. The v4 path
ended in a bare `return 0`, so the PASS/FAIL verdict had only ever been
printed — the command could not fail, which is why it exited 0 at 57% during
the re-fit trials. It returns 1 on failure now, verified both ways: PASS /
exit 0 at the shipped 70% threshold, FAIL / exit 1 with the threshold
temporarily raised to 95%. `weapon_only` stays printed beside it, reported
not gated, labelled the pre-re-fit unit. Docstring, CLAUDE.md and
HANDOFF.md updated; HANDOFF's "THE UNIT RE-FIT (blocked)" entry closed.

**Evidence.** GATE PASS at 87% (20/23). Full battery green with the gate
live.

### 2026-08-29 — Real party rosters from the killboard

**Context.** The data to see a killer's whole party existed and was unused.
`GroupMembers`: every official gameinfo kill event carries the KILLER'S
PARTY at kill time, each member with equipment, plus `Participants` (who
damaged the victim). albionbb strips both fields; the official API keeps
them. A real party roster — the exact unit the engine models — at killboard
scale, against a corpus of 36 published compositions. A standing CLAUDE.md
note was WRONG and corrected: "the official gameinfo events endpoint 504s
constantly" (2026-08-13) — re-tested 2026-08-29: events LIST, battles LIST
and single-event detail all returned 200 in under a second, first try, and a
25-event probe succeeded 25/25 with retry. albionbb stays preferred for
DISCOVERY as the only source with a `minPlayers` filter.

**Changes.** `pipeline/sample_parties.py` (network step, never part of a
build), three steps because each previous one cannot answer the question:
(1) DISCOVERY — albionbb `/battles?minPlayers=N`, the size filter (fight
size comes from the battle LIST `totalPlayers`; kill events carry no size);
(2) ROSTER — official `/battles/{id}`, EVERY player in the fight, no
equipment, the honest DENOMINATOR; (3) PARTIES — official `/events/{id}` per
kill, `GroupMembers` with gear.

**Evidence.** FIRST HARVEST: 39 battles → 193 distinct parties, 116 of size
5+, 184 with every member's weapon known, median per-battle coverage 0.815;
7 full 20-mans, 9 nineteens, 9 eighteens; 109 parties of 5+ carry a complete
weapon list — three times the published corpus. Two bugs caught by numbers
that could not be true, both properties of the data: (1) coverage came out
at 1.061 — `GroupMembers` reports the killer's WHOLE party including members
NOT at that battle (a 20-man party with 8 present still lists 20); coverage
now intersects the seen set with the official battle roster, and the
remainder is reported separately as `party_members_not_in_this_battle`;
(2) one squad was counted five times — a party loses members as they die,
so a single 19-man emitted 19/18/15/14/13-member arrays; parties are now
clustered by member OVERLAP (>50% of the smaller set) and the LARGEST
observation wins (14 "parties" → 9 real squads on the validation batch).
Caveats carried in the artifact's `semantics` field: only parties that got a
kill appear (revised below); a party is NOT a comp (a 300-player battle is a
coalition of parties, and the party is the useful unit); DISPLAY/EVIDENCE
ONLY, never a scoring input. Also measured (302-player battle): albionbb
killer+victim sees 76% of the roster; official battle detail sees 100% but
carries no equipment; the per-event union reached 54% WITH equipment from
only 36 of 182 events, adding 42 players the killer+victim route cannot
see. `party_cache/` gitignored; `out/party_rosters.json` committed (83 KB,
LF, no BOM).

### 2026-08-29 — The party sample's bias, measured

**Decision.** Parties that never got a single kill are not worth capturing
(their party information is not needed). Accepted, then checked — and
"winner-biased by construction" OVERSTATED the problem.

**Evidence.** The filter is not "won the fight": `GroupMembers` is emitted
on ANY kill, so a party that traded a few and got wiped is captured. Across
354 captured parties (5+ members present):

```text
   dominant   (2x+ K/D)                216   61%
   traded     (1-2x)                    68   19%
   wiped-ish  (more deaths than kills)  70   20%
   totals: 3785 kills / 1570 deaths, K/D 2.41
```

Losing parties are well represented. What drops out is the 10% of players
in no captured party at all: 474 players holding 34 kills against 475
deaths (K/D 0.07) — not a composition being tested. Coverage is 90% of all
players across the sampled battles. The artifact's `semantics` field and the
module docstring were rewritten from "WINNER-BIASED" to the measured
statement. The ZvZ meta from 38 fully-geared 15-20 man parties (677 members;
supersedes the kill-event reading, which saw only players who killed or
died):

```text
 #  weapon              slots  per party  in % of parties  seat
 1  Hallowfall             74      1.95         97%       main_healer
 2  Realmbreaker           35      0.92         68%       sustained_brawler
 3  Permafrost Prism       35      0.92         76%       ranged_aoe
 4  Dawnsong               34      0.89         66%       anti_heal
 5  Spirithunter           26      0.68         63%       pierce
 6  Polehammer             25      0.66         55%       engage_tank
 7  Bedrock Mace           23      0.61         39%       stopper_tank
 8  Blight Staff           22      0.58         50%       main_healer
 9  Spiked Gauntlets       21      0.55         55%       sustained_brawler
10  Lifecurse Staff        21      0.55         47%       purge
```

Hallowfall is in 37 of 38 ZvZ parties (97%) at ~2 per party — the
strongest single signal in any evidence layer. How much attribution bias
distorted: Longbow ranked 3rd by kill events, 12th here (29% presence — a
few squads stack several, most bring none); Battle Bracers 4th → 15th; and
Bedrock Mace, Oathkeepers, Blight Staff, Fallen Staff and Great Arcane Staff
appear in the party top-20 while absent from the kill-event top-20 entirely
— support and tank weapons whose users survive. The two columns answer
different questions: `slots` counts bodies (Longbow 20 slots at 29%
presence — stacked when taken), `in parties` counts how many teams chose it
(Earthrune 19 slots at 45% — one per party, taken more often). Small/mid
parties (5-14) are a different game: Wailing Bow, Frost Staff, Bear Paws and
Claws appear there and never in the ZvZ list.

### 2026-08-29 — Party harvest scaled 39 → 212 battles

**Evidence.** How much ZvZ there is: a discovery-only crawl of the albionbb
battle list (220 pages, no per-event fetches) found 4,400 battles of 20+
players in 8 days — 550/day at 20+, 260/day at 30+, 108/day at 50+, 34/day
at 100+; ~7,800 fights of 30+ a month. The first harvest of 39 battles was
about half of one percent of a single day's ZvZ. Scaled to 212 battles: 955
distinct parties, 656 of size 5+, 912 with every member's weapon known,
median coverage 0.851; ZvZ parties (15-20, fully geared) 38 → 290. THE FEED
ONLY REACHES BACK 8 DAYS even at 220 pages (oldest 2026-08-22) — a monthly
picture has to be accumulated going forward. The seat structure HELD across
a 7.6× sample increase:

```text
                    38 parties   290 parties
   pierce              2.76         3.16
   main_healer         3.29         3.16
   engage_tank         2.11         1.89
   shield_support      1.34         1.76
   stopper_tank        1.24         1.21
```

What kind of support matters:

```text
   pierce          3.16/party   50% of the support block   in 97% of parties
      Spirithunter, Battle Bracers, Occult Staff, Damnation Staff
   shield_support  1.76         28%                            84%
      Great Arcane, Rootbound, Enigmatic, Arcane Staff
   anti_heal       0.85         13%                            58%
      Dawnsong, Rotcaller
   purge           0.43          7%                            39%
      Lifecurse Staff
   zone_support    0.12          2%                            12%
```

Pierce is half of what "support" means in practice, and only two seats sit
at ~97-100% of parties: main_healer and pierce — resist-shred is as
non-negotiable as healing, a stronger statement than any template made.
ZvZ weapon meta (290 parties, 5,164 members): Hallowfall 2.12/party in 94%
(held from 97% at n=38), Realmbreaker 0.89/74%, Battle Bracers 0.73/29%,
Permafrost 0.73/60%, Spiked Gauntlets 0.70/70%, Spirithunter 0.59/57%,
Bedrock Mace 0.58/37%, Dawnsong 0.56/45%, Oathkeepers 0.53/50%. The bigger
sample changed: Battle Bracers 3rd by bodies but only 29% presence (slots
and presence must not be collapsed); Spiked Gauntlets 9th → 5th;
Oathkeepers 9th and off_tank in 51% of parties — a real seat the 38-party
read dismissed. 212 battles is ~0.4% of a week's 30+ fights: signals at 90%+
presence will hold; anything separated by a few points will not.
Display/evidence only.

### 2026-08-29 — Observed builds, and the role book tested against them

**Context (a correction first).** The party harvest had been described as
"real parties with full gear". It was not. Measured per event role: Killer
7/8 equipment slots filled, item power ~1300; Victim 7/8; Participants 7/8;
GroupMembers 1/8 — MainHand only, AverageItemPower 0. `GroupMembers`
carries WEAPONS ONLY; the 955 parties were weapon rosters, not builds. Full
kits live on Killer / Victim / Participants, so the sampler now takes party
STRUCTURE from GroupMembers and BUILDS from the combat roles. A member who
never killed, died or dealt damage yields a weapon and nothing more, and is
recorded that way rather than guessed. The cache gained a `schema` field;
schema-1 records (weapons only) re-fetch automatically.

**Evidence.** HARVEST: 270 battles → 1,283 parties (865 of size 5+) and
9,569 OBSERVED BUILDS, 9,261 of them with 6+ equipment slots, armour
evidence on 132 weapons. Slot fill: MainHand/Head/Armor/Shoes 99%, Cape 98%,
Potion 93%, Food 60% (food is eaten and not renewed), OffHand 29%
(two-handers occupy both hands — a real fact). Builds carry tier AND enchant
(`T6_ARMOR_CLOTH_AVALON@2`), which the published corpus mostly does not.
For scale: the curated evidence base was 625 gear pieces across 36 comps.
THE ROLE BOOK VALIDATES, independently — `roles.yaml` seats were curated by
hand; armour class is the standing role tell (cloth is a strong indicator).
Across 8,752 builds whose weapon has an assigned seat:

```text
   class         n     cloth  leather  plate
   frontline   1247      6%      9%     85%
   healer      2068     72%      9%     19%
   dps         2848     46%     46%      9%
   support     2589     23%     42%     35%
```

Frontline 85% plate, healers 72% cloth — the curation matches how people
gear, from a source that had no part in making it. DPS splitting evenly
cloth/leather matches play (cloth for long-channel burst, leather for brawl
DPS). Support at 35% plate is the odd one: a third of the "support" class is
armoured like frontline, which suggests the class holds two jobs and may
want splitting. ONE CONTRADICTION, n=93: Nature Staff is seated
`main_healer` but 53% of its users wear PLATE (49 plate / 35 cloth / 9
leather); every other healer weapon is cloth-dominant — either it is played
as a plate frontline healer or the seat is wrong. OPEN. Role-book gaps
(real usage, no seat assigned):

```text
   Brimstone Staff      n=75   cloth 100%    IP 1457
   Dual Swords          n=72   leather 86%   IP 1218
   Kingmaker            n=55   leather 64%   IP 1395
   Heron Spear          n=54   leather 44%   IP 1316
   Great Nature Staff   n=43   cloth 63%     IP 1182
   Holy Staff           n=36   cloth 86%     IP 1146
```

Brimstone at 100% cloth over 75 builds is exactly the burst-damage profile
recorded on 2026-08-27 (Scholar Robe + Assassin Hood: no defence, worn for
burst damage on a long-channel spell) — a seat the role book does not have,
now with evidence behind it. ARTIFACT SIZE flagged: `party_rosters.json` is
4.44 MB (was 83 KB), essentially all per-build gear; `pipeline/out/*.json`
is committed on purpose as parsed game data, but this is a growing
OBSERVATIONAL file. If that becomes unwelcome the split is obvious — commit
the aggregates (`weapon_armour`, `parties`, `summary`) and gitignore the raw
`builds` array — not done unilaterally because the raw builds ARE the
evidence. Display/evidence only: nothing here feeds scoring.
