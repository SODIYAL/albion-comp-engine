# Target Is The Median — implementation plan (2026-09-10)

Status: implemented 2026-09-10 (branch `target-median`). Deviations are
listed at the top of the spec; the four-stage `min` line was added to Tasks
1, 3, 4 and 5 mid-execution.

**Goal:** Every capability target — harvest band rows and hand-fitted content rows — becomes the median of what winning parties field, the board labels it as "typical", and thin rows say so.

**Architecture:** The scoring formula is untouched; only the number `target` holds moves. The harvest derivation (`derive_style_bands.py`) writes p50 instead of 0.9×p10 and gains a pooled `balanced` cell; the engine (Python + JS port) lets `balanced` read its band and exposes a descriptive `target_source(cap)`; the content templates are re-fit to the median of their fitted comps by a new reusable script and carry a `fit:` block; the dashboard relabels and chips rows from `target_source`, and the SIZE stepper follows the roster.

**Tech stack:** Python 3 (`py -3`), Node (parity + codec tests), YAML templates, single-file dashboard built by `dashboard/build.py`.

**Spec:** `notes/specs/2026-09-10-target-is-the-median-design.md`

## Global constraints

- Windows: `py -3` only; every committed-artifact writer opens with `newline="\n"`; never pipe a build through `grep`/`Select-Object` where `$LASTEXITCODE` is read.
- `engine/app_scoring.js` reads as binary to ripgrep — search with `Select-String`, read with an editor.
- Tests are script-style: run directly, exit 0 = pass; read the output.
- Engine change = both `engine/engine.py` and `engine/app_scoring.js`, then `py -3 tests/test_js_parity.py`.
- Never hand-edit `dashboard/index.html` / `docs/index.html` / `templates/style_bands.yaml` / `out/*.json` — they are generated.
- Descriptive layers never score: `target_source` is display provenance only; nothing reads it in a scoring path.
- No invented numbers: a row without a measured median keeps its current value and is labelled.
- The raw party cache is NOT on this machine: `audit_style_rosters.py` cannot run here. Code it, test it on a fixture, and record that the harvest machine regenerates the board.
- The tree carried unrelated uncommitted work (F29 need-bound fix, 2026-09-10). Not reverted; not folded into these commits — each task commits only the files it touches.

---

### Task 1: Harvest derivation writes the median (+ pooled `balanced` when present) — done

- Files: `pipeline/derive_style_bands.py` (constants at lines 63-72, docstring, header lines, row loop at lines 148-170); create `tests/test_style_bands_derive.py`.
- Interfaces: `derive_style_bands.derive(board_doc) -> list[str]` (the YAML lines; `main()` becomes load → `derive` → write) so a test can run it on a fixture without touching `out/`. Convention constants `TARGET_OF_P50 = 1.0`, `SOFT_OF_P90 = 1.15`, `MIN_DISTINCT = 40`. `STYLES` gains `"balanced"` at the front; `PARENT` unchanged (balanced has none). YAML output: `convention: {target_of_p50: 1.0, soft_of_p90: 1.15, min_distinct: 40}`; the per-row comment keeps `p10/p50/p90`; a row whose p50 is 0 writes `{soft_cap: X}   # p10/p50/p90 …; most winners field none, content target stands`. `ZERO_SHARE_MAX` removed entirely (constant, header line, convention line).
- Contracts (`tests/test_style_bands_derive.py`, on a fixture board): D1 target is the median (p50), not 0.9 × p10; D2 soft cap stays 1.15 × p90; D3 a >5% zero share no longer suppresses the target; D4 p50 == 0 writes the soft cap only, content target stands; D5 zero p90 writes no row; D6 the convention line names p50 and carries no zero-share knob; D7 the balanced cell is emitted like any style; D8 a thin cell borrows its nearest filled band; D9 the row comment keeps p10/p50/p90 for the reader.
- Header text: a zero p90 is unknown, never a number; a zero p50 (most winners field none) writes the soft cap only and the content target stands; the convention (2026-09-10): target = 1.0 × p50 — the TYPICAL winner, the number to aim for — and soft_cap = 1.15 × p90; until 2026-09-10 the target was 0.9 × p10, the LEAST any winner fielded: every row overshot and the score gave full credit at the floor (one healer "covered" 15 people); `balanced` reads the POOLED cell (every winner at the size, any style) when the audit has written one. Docstring: the 2026-09-09 zero-share rule is retired with the p10 convention it protected — the median does not sit on the edge of the zero mass (kite|10-14 heal_burst: 10% zeros, p50 5.8, stable across folds); before the audit runs on the harvest machine the pooled cell is absent and balanced keeps the content row.
- Steps: (1) the failing test (`AttributeError: derive`). (2) refactor `main()` into `derive(ev)` and switch the convention; the old per-cell `summary` print replaced by echoing the band and borrowed lines. (3) `all derive contracts pass`. (4) regenerate the real YAML from the committed board and check: kite 15-19 heal_burst reads `target: 10.26, soft_cap: 18.92`; no `balanced:` block appears (the committed board has no pooled cell yet). (5) commit `Style bands: target is the harvest median (p50), not 0.9 x p10`.

### Task 2: The audit writes a pooled `balanced` cell — done (code), deferred (data)

- Files: `pipeline/audit_style_rosters.py:342-392` (the per-style × band aggregation loop).
- Interfaces: `out/style_roster_evidence.json` `board["balanced|<band>"]` with the same entry shape as every style cell, aggregated over EVERY roster in the band (labelled or not). Only runnable on the harvest machine; here only the syntax check and Task 1's fixture test cover it.
- Change: `styles.append("balanced")` after the labelled styles (the POOLED cell: every winner at this size, whatever it was playing — labelled and unlabelled rosters alike; same dedupe, same stats, same `MIN_DISTINCT` downstream); the roster filter becomes `(style == "balanced" or r["style"] == style) and lo <= r["size"] <= hi`. The `entry["current"]` block's `e.set_content(content, ref, style)` already accepts `balanced`.
- Steps: (1) the change. (2) `py -3 -m py_compile pipeline/audit_style_rosters.py` (the script cannot run here). (3) record the deferred step in `BACKLOG.md` under "Engineering work, unblocked": regenerate the evidence board on the harvest machine (`audit_style_rosters.py`, `derive_style_bands.py`, `build_dataset.py`, the gates) so `balanced` gets its pooled median rows; until then `balanced` keeps the median-fitted content rows, labelled `content` on the board. (4) commit `Style roster audit: pooled balanced cell (every winner at the size)`.

### Task 3: Engine — `balanced` reads its band; `target_source(cap)`; parity — done

- Files: `engine/engine.py:381-413` (band block in `set_content`), a method after `soft_cap` (line 763); `engine/app_scoring.js:334-363` (band block), `targetSource` next to `softCap`; `tests/test_forge.py` (new `t_target_source`, registered after `t_min_need_disjoint_seats()`); `tests/test_js_parity.py:191` area (payload) and the compare loop at ~line 285; `tests/js_parity_runner.js:95` area.
- Interfaces: `Engine.target_source(cap) -> str` in `{"harvest", "harvest_borrowed", "content", "content_min"}`; JS `targetSource(cap)` identical. `Engine.band_row` may now be set for `style == "balanced"`. Reads `self.template.get("fit")` = `{"comps": int, "stat": "median"|"minimum"|"none"}` (Task 4 adds it to the YAML; absent → treated as `"minimum"`, i.e. `content_min`, so the engine stays honest on a pre-fit dataset).
- Contracts (F30): F30a a harvest-targeted row reports `harvest`; F30b a soft-cap-only harvest row falls back to the content row's provenance; F30c every source is one of the four words; F30d balanced reads its pooled band iff the board carries one; F30e below `min_size` every row is content-sourced; F30f kite at 15 asks for more than one healer's heal_burst (`target("heal_burst") > 8.0`).
- Engine: the band block sets `_target_src[c]` = `content` (fit stat median) or `content_min` for every requirement, then for the matching band row (any style in `bands`, including `balanced` once the YAML carries it, at `min_size`+) overrides target/soft cap scaled from `ref_size` and marks `harvest` / `harvest_borrowed` (when `borrowed_from` is set); a soft-cap-only row keeps the content target and takes the harvest soft cap when it clears that target. Rows are measured per style, so `target_mults` do not stack on them; hard floors and weights untouched; below `min_size` the content row stands; the cell is exposed as `band_row` for display, never a second scorer. The only behavioural change beyond `_target_src` is none: the old condition was already `style in bands["bands"]` — `balanced` was never excluded by code, only by the YAML having no `balanced` key, so the balanced behaviour arrives with the data. JS mirrors the block and `targetSource` in the surrounding method-definition style.
- Parity: `"target_source": {cap: e.target_source(cap) for cap in e.reqs}` in the Python payload next to `size_bucket`, compared in the loop; the runner emits `target_source` via `e.targetSource(cap)`.
- Steps: F30a-f fail (`AttributeError: target_source`) → implement both ports → forge all PASS (F30d reports `has_pool=False`, band_row None — passes), parity `60/60`, golden (no flips expected: the dataset is not rebuilt yet) → commit `Engine: target_source() provenance; band rows documented as medians`.

### Task 4: Content rows re-fit to the median of their comps; `fit:` blocks; build validation — done

- Files: create `pipeline/refit_content_targets.py`; modify `pipeline/templates/blackzone_roam.yaml`, `castle_outpost.yaml`, `roads.yaml`, `territory_defense.yaml`, `castle.yaml`, `faction_war.yaml` (a `fit:` block under `validated_sizes`; targets rewritten in the first two); `pipeline/build_dataset.py:2428` (validate `fit`); `pipeline/audit_dressed_templates.py` — no code change, re-run first (it was stale at 2026-09-02 and the 16 `zvz_20man` comps landed after).
- Interfaces: `refit_content_targets.py [--apply]` reads `out/dressed_template_audit.json`, groups parties by `content`, computes per capability the median of `supply.dressed` scaled to `base_size` per person for `scales: true` rows (raw for `scales: false`), prints a table `content cap old new n`, and with `--apply` rewrites only the `target:` number on matching requirement lines in the template YAML (regex on `^(\s+<cap>:\s*\{target:\s*)([0-9.]+)`, exactly one match per capability or the script exits), leaving comments and every other field intact. Contents with fewer than `MIN_COMPS = 3` distinct comps (by `comp` id) are printed and skipped; a capability whose median is 0 keeps its current target (never invent a number). `fit:` block: `fit: {comps: N, stat: median|minimum|none, source: dressed_template_audit, date: "2026-09-10"}`; `build_dataset.load_templates` fails closed unless `stat` is one of the three words and `comps` is an int ≥ 0.
- `fit:` values: blackzone_roam `comps: 18, stat: median`; castle_outpost `comps: 3, stat: median`; territory_defense `comps: 2, stat: minimum`; roads `comps: 1, stat: minimum`; castle `comps: 0, stat: none`; faction_war `comps: 0, stat: none`. The header comment: stat median = the median of `comps` published comps in the dressed audit (`refit_content_targets.py`); minimum = still the 2026-08-29 "0.9 x the least comp" fit (too few comps to take a median); none = no comp in the corpus, rows borrowed from a sibling content; the board labels minimum/none rows so a thin number is never read as what winners field.
- Steps: (1) refresh the dressed audit — `out/dressed_template_audit.json` carries 18 blackzone_roam parties (2 stated + 16 `zvz_20man`), 3 castle_outpost, 4 territory_defense parties from 2 comps, 1 roads. (2) write the refit script. (3) report first, read the table (sanity: castle_outpost heal_burst old 1.7 → new ≈ 2.9; the three comps field 2.6 / 2.9 / 3.0; `territory_defense: 2 comp(s) < 3 - left alone`, `roads: 1 comp(s) …`), then `--apply` and `git diff pipeline/templates/` — only `target:` numbers changed. (4) the `fit:` blocks by hand, six files. (5) validation in `build_dataset.py`. (6) the build chain bare (evidence_lint, build_dataset, build_cohort_families, dashboard/build.py — each exit 0), then forge, js_parity, golden, provenance, validation_modes, `tier2_blindtest.py v4`; every output read — F30b/F30f now run against median data; golden flips listed (case id, old pick, new pick), NOT re-pinned (Task 6); a `tier2 v4` drop below 70% is a stop-and-report. (7) commit `Content rows: targets re-fit to the median of their comps; fit provenance per template` (every `out/*.json` the rebuild rewrote included — the provenance test pins byte-identical rebuilds).

### Task 5: Dashboard — "typical" label, provenance chips, pick-card fix, SIZE follows the roster — done

- Files: `dashboard/_shell.html:1658` (section label); `dashboard/_app.js:1380-1426` (board rows/legend), `:427-440` (`whySentence`), `:37-40` (`syncEngine`), `:569-585` (extrapolated notice); `dashboard/_decision_layer.css` or `_shell.html` styles (a `.tag.src` chip); `tests/test_dashboard_layout.py` (new L20 block before the FAILURES summary).
- Interfaces: consumes `ENG.targetSource(cap)` from Task 3; page helper `const targetSource = cap => ENG.targetSource(cap);` beside `target`/`softCap`.
- Contracts (L20 — the board shows the TYPICAL winner, and says when it cannot): L20a the section label says typical winner; L20b the page reads provenance from the engine; L20c the board chips content_min / borrowed rows from `targetSource` (`class="tag src"`); L20d the legend value is labelled typical; L20e the lead gap never appears in "already covers"; L20f the SIZE stepper follows roster growth on every path (`PLANNED = Math.max(PLANNED, party.length)` in `syncEngine`). Rationale recorded in the test: the second number on every row was the least any winner fielded, labelled "target"; every row overshot and the reader concluded three healers at 15 was too many; the chip is the ENGINE's word, never a page-side rule.
- Implementation: label `Capability supply vs. typical winner`; board rows compute `src = targetSource(c)` and render a `min` chip for `content_min` (title: no measured median for this row at this content — the old minimum, the least any fitted comp brought; read it as a floor, not as what winners field) or a `~` chip for `harvest_borrowed` (thin harvest cell — the median is borrowed from the nearest band of the same style); the legend value reads `have / typical` and the ring title `<cap> <have> / typical <t>`; the `styleTag` title now reads "the typical number is measured per style at 10+, so it already reflects how this style fights; below 10 it is the content row"; chip style `.tag.src{color:var(--muted,#9aa3b2);border:1px dashed currentColor;opacity:.8}`; `whySentence` filters the lead capability out of `strong` and says "at X of the typical Y units"; `syncEngine` starts with `PLANNED = Math.max(PLANNED, party.length)` (the header read 7 while eleven were seated — judgement was already at roster size, only the stepper lagged, and only on the manual paths); the extrapolated notice reads "At N the typical numbers come from the harvest median for this style (10+) or the content row scaled per person; nothing here has been blind-validated at this size yet" and the chip title names the validated sizes.
- Steps: L20a-f fail → implement → rebuild, `test_dashboard_layout.py` all pass, `node tests/test_loadout_codec.js`, `node tests/test_live_party.js`, `node tests/test_display_math.js` exit 0 → visual check on the 15-man kite from the motivating case (`#c=castle_outpost&n=7&st=kite&p=...`): label reads typical winner; heal_burst reads `12 / ~9`; content-min rows wear `min`; the SIZE stepper shows 15 → commit `Board: supply vs. typical winner; provenance chips; the plan follows the roster`.

### Task 6: Golden flips → decisions; the records — done

- Files: `tests/VALIDATION.md` (index row), `notes/validation/2026-09b.md` (dated entry, append-only), `MASTERSHEET.md` (~line 94, the `tune:templates` section), `HANDOFF.md` ("The engine today"), `pipeline/README.md:387` paragraph, `BACKLOG.md`, `notes/specs/2026-09-10-target-is-the-median-design.md` (status → implemented with the deferred balanced note).
- Steps: (1) collect the golden flips from Task 4 step 6: per failing case — case id, content/size/style, party, old expected pick, new top pick, the two capability rows that drove it (`explain()`), one line each; "none" if none. (2) put them through a validation round: the caller's pick is collected BEFORE the engine's is shown; then per case: re-pin (the caller agrees with the new pick), keep the old pin plus an override with its citation (the caller disagrees), or open question (logged under "Open questions", not advanced). (3) the records — `notes/validation/2026-09b.md` entry "Target is the median: the board stops calling a floor a target": what was wrong (every target was 0.9 × p10, the least any winner fielded, and the score gave full credit there — one healer "covered" fifteen, a second earned ~0.3 of 6 points; the measured median was in the YAML comments and used by nothing); the decision (target = p50 on harvest bands, every style × band; soft cap 1.15 × p90 unchanged; one smooth curve below; content rows re-fit to the median of their comps where >= 3 comps exist — blackzone_roam 18, castle_outpost 3 — left and LABELLED otherwise — territory_defense 2, roads 1, castle/faction_war 0; `balanced` reads a pooled harvest cell once the audit runs on the harvest machine; board "supply vs. typical winner" with the `min` chip from `target_source()`; SIZE stepper follows the roster); before/after kite 15 (ref 17 scaled): heal_burst 4.6 → 9.1, heal_sustain 4.9 → 10.0, cleanse 1.6 → 5.3, tankiness 34.9 → ~42; probe: a 14-man kite with two healers moved Hallowfall #26 → #9; the golden flips and their decisions; gates (forge F30a-f, parity 60/60 + target_source, tier2 v4 percentage); anti-circularity — the bands are derived from the harvest; the gate results are hypotheses for a maintainer decision, not a retuning input. `tests/VALIDATION.md` index row: 09-10 — target = p50 on every row (bands + content re-fit), soft cap 1.15 x p90, one curve below; `balanced` pooled cell deferred to the harvest machine; board says typical + `min` chips | derive_style_bands.py, templates/*.yaml fit:, engine target_source | F30, L20, the golden ids | 09b, Target is the median. `MASTERSHEET.md` `tune:templates`: targets are the TYPICAL winner (harvest median, p50) — style x size rows for every declared style at 10+, content rows below that (re-fit to the median of their comps where >= 3 exist; `fit:` in each template says which); soft caps are 1.15 x p90; a `target` set there overrides that number for one content and is read as a median. `pipeline/README.md:387`: the content rows were comp-fitted 2026-08-21, re-fitted to person units 2026-08-29 and to the MEDIAN of their comps 2026-09-10 (`refit_content_targets.py`, all rows together; each template's `fit:` block states comps and stat); territory_defense (2 comps) and roads (1) stay on the old minimum and say so; castle and faction_war rest on no comps. `HANDOFF.md` "The engine today": one paragraph on the median convention, `target_source`, and the deferred balanced cell. `BACKLOG.md`: any item about targets overshooting / board readability closed; the Task 2 item kept. Spec status line updated. (4) the final full gate run — every line of the CLAUDE.md test list, bare, all exit 0, each result reported with its printed tail, not a count. (5) commit `Validation: target is the median (2026-09-10) — decision records, golden re-pins`.

---

## Self-review

- Spec: derivation, zero-share retired, balanced pooled → Tasks 1, 2; engine, balanced reads band, target_source, parity → Task 3; content re-fit, fit blocks, build validation → Task 4; board label, chips, whySentence, SIZE stepper, notice text → Task 5; gates, golden flips as decisions, records → Tasks 4.6 and 6. Out-of-scope items untouched.
- Names: `target_source` / `targetSource`, `_target_src` / `_targetSrc`, `fit.stat`, `TARGET_OF_P50`, `derive(ev)` — used consistently across tasks.
- No placeholder steps; the one open lookup (the board function's name for the L20 anchor, found with `Select-String -Path dashboard/_app.js -Pattern "cap-rings"`) was stated with the command to resolve it.
