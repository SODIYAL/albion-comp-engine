# ILLUSTRATIVE — NOT CURATED, NOT EVIDENCE-LINTED.
#
# These are the design-doc §2.3 hand-sketched numbers that the throwaway
# prototype (tests/prototype_engine.py) used to validate the model shape. They
# exist here for exactly one reason: to keep the 9 golden tests runnable while
# curation is in progress. They are NOT a data release.
#
# Every entry lacks an `evidence` field, so `evidence_lint.py` would reject all
# of them — that is intentional and correct. `build_dataset.py` refuses to mark
# a release clean while any illustrative sheet is present.
#
# CURATION PATH: when a weapon here gets a real curated sheet in ../ (with
# evidence spells), delete its block from this file. The loader gives curated
# sheets precedence, so a leftover block is shadowed rather than harmful — but
# delete it anyway so the illustrative count reports the truth.
#
# Weapon keys resolved against pipeline/out/weapon_lines.json (2026-08-12).

# MAIN_ARCANESTAFF_UNDEAD (Witchwork) — CURATED 2026-08-12, see ../witchwork.yaml.
# Its illustrative block claimed energy_drain 2 — FABRICATED: nothing in the
# current kit drains energy (verified against every equippable spell). Its
# heal_reduction 1 was tagged GEAR and belongs on gear sheets. Curation found
# the real identity instead: group cleanse (W), ally empowerment, Black Hole clump.

# 2H_ICECRYSTAL_UNDEAD (Permafrost Prism) — CURATED 2026-08-12, see
# ../permafrost_prism.yaml. Its illustrative block claimed clump_create 2,
# zone_control 3, tankiness 1 and mobility 1; curation dropped clump_create
# (nothing stacks enemies), tankiness (Frost Lance shreds, it doesn't shield)
# and mobility (teleport filed as disengage), and downgraded zone_control to 2.

# MAIN_HOLYSTAFF_AVALON (Hallowfall) — CURATED 2026-08-12, see ../holy_line.yaml
# Its illustrative block claimed `cleanse 2`; curation found no cleanse anywhere
# in the holy kit. Removed rather than shadowed.

# 2H_HOLYSTAFF (Great Holy) — CURATED 2026-08-12, see ../great_holy.yaml.
# Its illustrative cleanse 2 was the already-settled holy error (no holy
# weapon cleanses except Lifetouch/Fallen E). heal_burst downgraded to 2
# (the E is a sustain channel); the E's CC-resist-ignoring 10m shove was
# never scored — knockback_displace/peel 2 added.

# 2H_NATURESTAFF_HELL (Blight Staff) — CURATED 2026-08-12, see ../nature_line.yaml

# 2H_HAMMER (Great Hammer) — CURATED 2026-08-12, see ../great_hammer.yaml.
# Curation confirmed engage 3 / clump_create 3 / stun 2 / tankiness 2,
# downgraded zone_control to 1 (Polehammer W-pool parity), upgraded peel to 2,
# and added the QW conditionals + knockback_displace (E structured, verified).

# 2H_RAM_KEEPER (Grovekeeper) — CURATED 2026-08-12, see ../grovekeeper.yaml.
# clump_create 2 removed (air-throw holds a clump, it does not create one —
# the Permafrost/Kingmaker ruling); peel upgraded to 2; knockback_displace
# added (scaling CC-resist-ignoring air-throw, structured, verified).

# 2H_DAGGERPAIR (Dagger Pair) — CURATED 2026-08-12, see ../dagger_pair.yaml.
# burst_st 3 confirmed; execute downgraded to 1 (Slit Throat scales with
# charges, not target health — the clean execute is Bloodletter's E);
# mobility upgraded to 2 (dagger pool parity); heal_reduction 1 added
# (Slit Throat reduces Healing Received — verified text).
#
# ALL EIGHT ILLUSTRATIVE BLOCKS ARE NOW CURATED. This file is retained as
# the historical record of the §2.3 prototype numbers and their corrections;
# it contributes nothing to the dataset.

# MAIN_RAPIER_MORGANA (Bloodletter) — CURATED 2026-08-12, see
# ../bloodletter.yaml. Curation confirmed all five illustrative scores and
# added the Q/W conditionals (stun/peel via Shadow Edge, shred via Sunder Armor).

# 2H_HARPOON_HELL (Spirithunter) — CURATED 2026-08-12, see ../spirithunter.yaml.
# Its illustrative block claimed heal_reduction 3 — FABRICATED: nothing in the
# kit reduces healing (verified against every Q/W/E/passive description).
# buff_allies 1 also had no basis. Same error class as Hallowfall's cleanse.
# The weapon's real strength (Cripple's -25% damage dealt) became scoreable
# when damage_debuff was promoted into §2.2 (2026-08-12) — see the curated sheet.
