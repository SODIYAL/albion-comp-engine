# Wailing Bow — curated 2026-08-12 from ao-bin-dumps spell text (curate_helper.py).
# Usage: 3 sightings, dominant role: range.
#
# Identity: the clump-line nuke. The E (Demon Arrow) pierces everything on a
# line with damage RAMPING per enemy already hit — negligible into one
# target, devastating through a stacked clump. Pure anti-clump artillery;
# burst_aoe 3 is the weapon's entire reason to exist. Shares the bow Q/W
# pool; QW-conditional scores mirror the Mistpiercer/Badon sheets.

- weapon: 2H_BOW_HELL                # Wailing Bow — the clump-line nuke
  curated_as_of: 2026-08-12   # staleness check vs out/patch_history.json
  role_hint: range
  capabilities:
    - {cap: burst_aoe,     score: 3, evidence: HELL_ARROW}        # E: ramping line pierce — anti-clump payload
    - {cap: sustained_dps, score: 2, evidence: WEAPON_STATS}      # bow AA pattern between Es
    - {cap: root,          score: 1, evidence: GROUNDARROW}       # W: Ray of Light (QW)
    - {cap: catch,         score: 1, evidence: GROUNDARROW}       # (QW)
    - {cap: peel,          score: 1, evidence: GROUNDARROW}       # (QW)
    - {cap: slow,          score: 1, evidence: PASSIVE_SLOWPOISON}  # every AA slows; Q: Multishot
    - {cap: resist_shred,  score: 1, evidence: DEADLYSHOT}        # Q: stacking -3% x3; Piercing Arrows passive (QW)
    - {cap: mobility,      score: 1, evidence: SPEEDSHOT2}        # W: +30% MS (QW)
    - {cap: disengage,     score: 1, evidence: JUMPSHOT2}         # W: Frost Shot self-knockback escape (QW)
