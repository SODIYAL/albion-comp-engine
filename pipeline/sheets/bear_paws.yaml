# Bear Paws — curated 2026-08-12 from ao-bin-dumps spell text (curate_helper.py).
# Usage: 10 sightings, dominant role: melee.
#
# Identity: diving AoE bruiser. The E (Razor Cut) is a CC-immune leap whose
# cooldown refunds on player hit, with 15% of damage dealt returned as Health —
# mobility, engage, disengage and self_sustain all hang off it. The Q3 combo
# finisher (Raging Leap) is a second leap that roots on landing.
#
# DRAFT CORRECTIONS (directionality rule):
#   - heal_sustain 2 REMOVED: Razor Cut's lifesteal heals the WIELDER only.
#   - buff_allies 1 + peel 1 via PASSIVE_ARMORCHANCE_AXE REMOVED: the passive
#     reads "YOUR damage resistances increase" — self-directed; the effect map
#     mis-tagged it as ally-directed. buff_allies instead grounds on Battle
#     Rush's ally-side +20% Healing Received.
#   - tankiness REMOVED entirely (was 2 in the draft): CC-immunity windows
#     are personal defensives, not frontline durability — the
#     momentary-defensive ruling (2026-08-12).

- weapon: 2H_DUALAXE_KEEPER          # Bear Paws — diving AoE bruiser
  curated_as_of: 2026-08-12   # staleness check vs out/patch_history.json
  role_hint: melee
  # deliberate non-takes of tree-pool spells (see sheets/pools/axe.yaml)
  except:
    - {cap: catch, evidence: AXEBOOST}
    - {cap: engage, evidence: AXE_CHARGE}
    - {cap: self_sustain, evidence: PASSIVE_HEALTHCHANCE_AXE}
    - {cap: mobility, evidence: RENDINGCOMBO}
    - {cap: heal_reduction, evidence: RENDINGSTRIKE}
  capabilities:
    - {cap: mobility,       score: 6, evidence: DUALAXE_CRAWLER}  # E: CC-immune leap, CD refund on player hit; Q3 Raging Leap
    - {cap: engage,         score: 4, evidence: DUALAXE_CRAWLER}  # commits into the clump CC-immune
    - {cap: disengage,      score: 4, evidence: DUALAXE_CRAWLER}  # same leap outwards
    - {cap: catch,          score: 4, evidence: DUALAXE_CRAWLER}  # repeatable gap-close
    - {cap: burst_aoe,      score: 4, evidence: DUALAXE_CRAWLER}  # 170 cone on impact; W: Internal Bleeding / Raging Blades
    - {cap: self_sustain,   score: 4, evidence: DUALAXE_CRAWLER}  # E: 15% of damage dealt as Health, AoE-fed
    - {cap: heal_reduction, score: 2, evidence: AXE_CHARGE}       # W: same cast, -20% Healing Received on enemies (QW)
